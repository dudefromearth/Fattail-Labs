# HOLD token — GROK BUILD — HEATMAP (GBH) seating

**ID:** `GBH-W0`  
**Instance:** **GROK BUILD — HEATMAP** (GBH) — third build instance  
**Program:** Options Lab Heatmap **Term Mass** (internal id `gex-cal`)  
**Spec:** [`Specs/FatTail-Labs-Options-Lab-Heatmap-GEX-Calendar-Spec-v0_1.md`](../../Specs/FatTail-Labs-Options-Lab-Heatmap-GEX-Calendar-Spec-v0_1.md) **v0.1 DRAFT**  
**sha1:** `36a347185aaece5c4b900bf22e9683ad4ffbaf6f`  
**Board:** `agents/p-options-lab-heatmap-gex-calendar/` (**DL-747**). Duplicate folder `p-options-lab-heatmap-gex-cal/` is not the board.  
**GO token (program):** [`agents/go/GC0-W0.md`](./GC0-W0.md) — unstamped. This file is seating, not GO.  
**DL:** **not filed** — Lima files on OD-GC1…GC4 stamp after India / Echo+Tango / Hotel, not from this seating.

**Status:** **HOLD.** Not BUILD AUTHORITY. Chat is not a stamp.

Coach (2026-09-18, seating): GBH owns the gex-cal Heatmap template track (spec line, `HM*` / `OD-GC*` tokens, `/app/options-lab/heatmap` route tree). Frozen `gex_v1` generations are the data domain. Reviews and gates route to **GROK ADVISOR** via Coach. DRAFT is not build authority — hold at flag/fixtures until Coach stamps **OD-GC1…GC4**.

---

## Preconditions (file must name these)

| Check | Value |
|-------|--------|
| Spec | GEX Calendar **v0.1** — **DRAFT** (not BUILD AUTHORITY) |
| Spec sha1 (whole file, at seating) | `36a347185aaece5c4b900bf22e9683ad4ffbaf6f` |
| Machine | **StudioTwo** only unless Coach names another |
| Frozen `gex` / `gex_v1` | **Stays** byte-identical. Read for formula parity. Do not restyle. |
| LIM / Width Fit / Advanced Fly | **Do not edit** those trees |
| VPS* / VPSB* / SADEV* | **Never edit** |
| VP stores / surfaces / `/app/options-lab/volume-profile` | **Never edit** |
| Shared Labs components | Change **only through Coach** (`HeatmapChainPanel`, `OptionsLabChrome`, suite nav) |
| MiniTwo | **Out** |

---

## OD-GC1…GC4 (stamp Accept or Override)

Until every row is stamped, **GC14**: flag, fixtures only, **no production switcher**.

| ID | Spec recommendation | Coach |
|----|---------------------|-------|
| **OD-GC1** Pack | Join expirations already listed on the Heatmap strip (HM2 attach). Fail loud when a listed expiry has no book. Do not wait for SSR-MEXP (archive) to draw live columns. | [ ] Accept · [ ] Override: |
| **OD-GC2** N columns | Visible = listed on the strip, typical ~5–10, not the entire calendar. | [ ] Accept · [ ] Override: |
| **OD-GC3** Peak chrome | Gold only in v1. Green/red = FI-GC1. | [ ] Accept · [ ] Override: |
| **OD-GC4** Member label | **Term Mass** (Coach 2026-09-18). Echo does not rename. No vendor name. | [x] **Accept** · [ ] Override: |

**OD-GC5** (layout enum `"matrix-profile"`) is a spec recommendation Coach may discard. Not a hold gate.

---

## On stamp (do not run until the four boxes above are marked)

1. India — spec / architecture (HM1 pack tension, isolation).  
2. Echo + Tango — chrome, label, honesty line.  
3. Hotel — GEX sign convention, no magnet/pin/air as official labels.  
4. Lima — DL (paste-ready in spec §16) + Arch 29 pointer.  
5. Spec status → BUILD AUTHORITY. Then Juliet may seed GC1 (flag/fixtures). Reviews of spec and gates → **GROK ADVISOR via Coach**.

---

## Do not

- Treat this file as GO.  
- Ship a production switcher entry.  
- Rewrite `gex_v1`. Restyle frozen `gex` or LIM.  
- Fake N columns from one expiry.  
- Fork a second scrubber (Time Machine owns replay).  
- Name ITMatrix in chrome, ids, or help.  
- Edit VPS* / VPSB* / SADEV* / VP / volume-profile.  
- `git add -A`. Stop StudioTwo `:3000` / `:4000`. MiniTwo.
