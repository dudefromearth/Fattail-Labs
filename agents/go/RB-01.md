# GO: RB-01

```
id: RB-01
status: GO
date: 2026-08-14
issued_by: Coach
```

**Coach GO (verbatim):**

> GO: RB-01

**Scope:** Complete local schema backout + living tripwire. Local `labs` DB;
`server/tests/`. Drop leftover `coach_lab_*` tables, delete the
`schema_migrations` row `128_coach_lab.sql`, land a pytest that fails if
`/api/admin/coach-lab/*` is registered. Dev / StudioTwo only.

**Basis:** Bill RB-01 (G1+G2). Coach narrowing 2026-08-14 (prior GO, retained):
MiniTwo migration head is **125**; MiniTwo never received **128**. This chat GO
does not name MiniTwo.

**Not in scope:** MiniTwo. Production SQL. Deploy. Conversation Lab
implementation (CL-1…CL-4 / CL-G). All other RB-* items. RB-10 SSH.

Never mint this file without Coach's chat `GO: <ID>` (DL-329). Chat alone is
not authority. `scripts/require_go.py --id RB-01` must exit 0 before work.

Conversation Lab implementation stays STOPPED until `agents/go/CL-1.md`
(and later CL-2…CL-4 / CL-G) exists with `status: GO`.
