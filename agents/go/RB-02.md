# GO: RB-02

```
id: RB-02
status: GO
date: 2026-08-14
issued_by: Coach
```

**Coach GO (verbatim):**

> GO: RB-02

**Scope:** Publish git SHA on `/api/health` (or `/api/version`) in
`server/main.py`. Health body contains a SHA that matches `git rev-parse HEAD`
on the host that serves the request.

**Basis:** Bill RB-02 (G2). This chat GO does **not** name MiniTwo. Production
SHA attestation remains **RB-10**. Local / StudioTwo verify only.

**Not in scope:** MiniTwo. SSH. Production deploy. Conversation Lab
implementation. Other RB-* items.

Never mint this file without Coach's chat `GO: <ID>` (DL-329). Chat alone is
not authority. `scripts/require_go.py --id RB-02` must exit 0 before work.

Conversation Lab implementation stays STOPPED until `agents/go/CL-1.md`
(and later CL-2…CL-4 / CL-G) exists with `status: GO`.
