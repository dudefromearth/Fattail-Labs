# GO: RB-05

```
id: RB-05
status: GO
date: 2026-08-14
issued_by: Coach
```

**Coach GO (verbatim):**

> GO: RB-03, RB-04, RB-05, RB-06, RB-07

**Scope:** Fail-loud unit tests for `LABS_ENV` / `LABS_PORT` /
`LABS_SESSION_SECRET` in `server/tests/`. Missing var raises `ConfigError`.

**Basis:** Bill RB-05 (G1+G2). This chat GO does not name MiniTwo.

**Not in scope:** MiniTwo. Production SQL. Deploy. Conversation Lab
implementation. Other RB-* items. Changing production boot defaults.

Never mint this file without Coach's chat `GO: <ID>` (DL-329). Chat alone is
not authority. `scripts/require_go.py --id RB-05` must exit 0 before work.

Conversation Lab implementation stays STOPPED until `agents/go/CL-1.md`
(and later CL-2…CL-4 / CL-G) exists with `status: GO`.
