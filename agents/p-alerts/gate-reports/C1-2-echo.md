# C1-2 — Echo Builder / holder

**Verdict:** **APPROVED**

## HIG / tokens-and-primitives

| Id | Result |
|----|--------|
| H1 | Kit `Modal`, `data-work-surface="dark"`, `radius.lg`, `elevation.3`. No `#2c2c2e`. |
| H2 | `IconButton` + `xmark`, name Close. Close-dot gone. |
| H3 | Kit `Button` Cancel plain / Save primary. |
| H4 | Type is `SegmentedControl` (4). |
| H5 | Tag chips and ±1 steppers are `min-h-[var(--hit-min)]`. Chip UI uses tokens. |
| H6 | Holder **+** is kit `IconButton` 44pt. Rows `inspectorRow` 44pt. |
| H7 | Empty holder: no copy, no EmptyState. |

No invented severity field. No delete.
