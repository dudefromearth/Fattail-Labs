# C2-G — Canvas alert apply

**Verdict:** **PASS** (as-built + H9 row height)

Right-click plot → `analyzer-alert-menu`. Blank → canvas price; tent hit → position (`nearestPositionOnExpiration`). Choosing a row calls `onCanvasAlert` / `onPositionAlert` → Alert Builder seed. Left-click still pans. Menu action rows `min-h-11` (H9). Preview dashed vertical while menu open.

Does not reopen Autofit / wheel / Packet A pan.
