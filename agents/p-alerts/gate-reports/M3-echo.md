# M3 — Echo Manager chrome

**Verdict:** **APPROVED**

## HIG / tokens-and-primitives

**H8:** `/app/alerts` Member dialect — grouped list, calm overview, kit `Banner` + `Button`. Not `/admin`. Settings Alerts uses kit `Banner`; **no** + Add Rule.
