# W0-3 — Echo grammar + HIG

**Project:** p-alerts  
**Agent:** Echo  
**Date:** 2026-08-20  
**Verdict:** **APPROVED**

## HIG / tokens-and-primitives (required)

H1–H9 are **packet work**, not polish.

| Id | Packet | Stamp |
|----|--------|-------|
| H1–H7 | **C1** | Kit Modal / xmark / Button / SegmentedControl (Type 4). 44pt chips/steppers/sub-tabs/holder/+. Empty holder = Coach deviation from EmptyState — do not add essay. Dark-pinned work-surface via tokens. Current dialog MSC chrome (`#2c2c2e`, close-dot) is **C1 debt**. |
| H8 | **M2** | `/app/alerts` Member dialect, grouped, kit List/Banner. Not `/admin`. |
| H9 | **C2** | Menu 44pt; **+** is a11y; no keyboard nav for context menu. After C2-0+BA only. |

Close is kit `IconButton` + `xmark`, not a close-dot. Position sub-tabs: live three (P&L / Profit / Greeks) as SegmentedControl (≤5); Coming soon as separate disabled 44pt controls (do not ship a 6-segment control).

Builder: no severity field v1. Holder: no delete. Manager index read-only.
