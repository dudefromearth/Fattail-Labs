# Seed M3-1 — Echo Manager chrome review

**Project:** p-alerts  
**Agent:** Echo  
**Phase:** M  
**Depends:** M2  
**Gate it feeds:** M-G

## Intent

Post-implement review of `/app/alerts` and Settings Alerts pane against W0-3.

## Asks

HIG: 44pt, sentence case, read-only index, no second builder, no profit theater in stats. **Member dialect** — grouped/calm, not `/admin`. Block only HI invariant.

## Named section (required)

**HIG / tokens-and-primitives** — tick plan **§8.5 H8** by id. Kit `List`/`Banner`/`Button`; Member dialect (not `/admin`); no raw hex/px/`zinc`. “HIG looks fine” without H8 → **RETURNED**. Delta M-G cites this section.

## Files in scope

M2 paths **read / comment**. No product edits unless a one-line kit fix; otherwise RETURN Charlie.

## Done when

`gate-reports/M3-echo.md` APPROVED or RETURNED **with the named HIG section**.
