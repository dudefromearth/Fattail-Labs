# Seed W0-3 — Echo Builder / holder / Manager chrome

**Project:** p-alerts  
**Agent:** Echo  
**Phase:** W0  
**Depends:** W0-2 APPROVED  
**Law:** HI Spec v1.0 · AZ-ALB §3–5 · ALM §2  
**Gate it feeds:** W0-G · Charlie C1 / M2

## Intent

Stamp member grammar for **C1** and **M**. Canvas Autofit / pan is **not** this packet (viewport boards + C2-2).

## Asks

1. Alert Builder: MSC **feature range**, Labs-typed (**position**). Kit `Modal` / `SegmentedControl` / `IconButton` `xmark` (not close-dot) / `Button` Cancel plain · Save filled. Algo/BE/Trail/0DTE visible, Save off. **No severity field in v1.**  
2. Holder: left inspector, header **+** (stands off), ~3–4 cards, scroll, **no** helper essay, info + Active/Idle (Unbound when dangling). **No delete.** Empty holder = **named Coach deviation** from `EmptyState` — do not RETURN to add an essay.  
3. Manager `/app/alerts`: **Member dialect** (grouped, calm). Read-only index + stats + deep link. **Not** a Risk-graph editor. **Not** `/admin` density.  
4. Settings → Alerts: configuration only. **No** + Add Rule. Honesty banner = kit `Banner`.  
5. Canvas gesture (left-pan / right-click alerts) is Packet A law; Echo reviews **C2 after C2-1**, not here. **+** is the a11y path for canvas apply — do not require keyboard nav on the context menu.  
6. **44pt** including chips, steppers, sub-tabs, holder cards, round **+**.  
7. Dark-pinned work-surface dialect is **declared** in AZ-ALB §4 — stamp or labeled override.  
8. Stamp plan **§8.5 H1–H9** as **packet work** (C1 / M2 / C2), not polish. Name the current dialog’s MSC chrome (`#2c2c2e`, close-dot) as **C1 debt**.

## Named section (required)

**HIG / tokens-and-primitives** — kit primitives in law; H1–H9 assigned to packets; 44pt; named deviations; Member vs work-surface dialect. Delta W0-G cites this section. A review that only says “HIG looks fine” is **RETURNED**.

## Files in scope

AZ-ALB v1.0.3 · ALM v1.0.3 · HI Spec v1.0 · plan **§8.5** · `AlertBuilderDialog.tsx` **read only** (name the MSC chrome as C1 debt).

## Out of scope

Implementation. `HostPnLChart` edits. Viewport Autofit. Inventing a “close dot” kit.

## Done when

`gate-reports/W0-3-echo.md` — verdict shape **plus** the named HIG section. Block only HI invariant.

## Invariants

Echo owns HIG. **FP14.** Charlie does not invent later. Coach holder laws stay.
