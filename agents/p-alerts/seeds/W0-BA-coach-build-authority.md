# Seed W0-BA — Coach BUILD AUTHORITY (Packet M and/or C1)

**Project:** p-alerts  
**Agent:** Coach  
**Phase:** W0  
**Depends:** W0-G PASS  
**Gate it feeds:** M1 and/or C1-1

## Ask

Stamp BUILD AUTHORITY for **named packets**:

- **Packet M** (Manager API + `/app/alerts` + Settings wire), and/or  
- **Packet C1** (Builder + adapter + holder)

**C2 is not in this stamp.** Alternate: stamp the plan and have Lima log a DL **naming bypassed W0 packets** (not a silent waive).

## Confirm

- Two planes  
- ALM §3.2 canonical (`suite` + `severity`)  
- Delete unshipped  
- C2 waits on viewport W-G  
- OD-ALM-eval in-tab (unless you override)  
- MiniTwo not in this fire

## AL-B1 (required if W0-G named **reachable**)

Canvas apply is live in the built app without a C2 gate. Choose **one** — silent omit is not lawful:

1. **Keep dark until C2-G** — Juliet fires `D0-1-charlie-dark-gate.md` (flag or unhook `contextmenu` / callbacks). Viewport harnesses must not treat the menu as product law. C2-G still required later.  
2. **Accept as-built** — Lima logs a DL: reachable on [host / git_sha]; **not** C2 GO; C2-G still required for AT-ALB-2/3/4/8.

If W0-G named **not-reachable**, skip this row.

## Out

Packet C2 BUILD. SMS/email live. Practice type catalogs. Viewport Autofit.

## Done when

`gate-reports/W0-BA.md`: BUILD AUTHORITY listing **M**, **C1**, or both — or RETURN.  
If W0-G named reachable, the stamp **names keep-dark or accept-as-built**.  
If AUTHORITY, Juliet fires the named packet’s first seed (and D0 if keep-dark).
