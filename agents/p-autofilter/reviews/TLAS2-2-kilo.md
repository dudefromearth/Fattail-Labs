# TLAS2-2 — Kilo

```
cd server && .venv/bin/python -m pytest tests/test_help_catalog.py tests/test_help_ai.py -q
→ 25 passed

GET /api/help/guides/trade-log-autofilter → published; body has Strategy; no “four columns”
```

Search “strategy autofilter trade log” hits stored-code / catalog-label copy.
