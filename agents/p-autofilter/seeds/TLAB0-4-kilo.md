# TLAB0-4 — Kilo — Book universe inventory

**Isolation:** read-only. Re-quote, do not “should work.”

**Do:** File+line: PAGE_LIMIT 80, `_TRADE_PAGE_DEFAULT` 80, `_TRADE_PAGE_MAX` 200, `_TRADE_BOOK_LIMIT` 10000, `applyAutofilter(trades`. Quote spec §1 MiniTwo Default first-80 vs 2022 counts. Confirm Trade Log fetch does not pass `years`/`days` today.

**Done when:** `agents/p-autofilter/reviews/TLAB0-4-kilo.md`.
