# TLAF0-4 — Kilo Trade Log Autofilter inventory

**Project:** p-autofilter  
**Agent:** Kilo  
**Gate:** TLAF0-G  
**Isolation:** read-only. Grep and quote.

**Do:**

1. Autofilter files / `autofilter-` testids.
2. `practice-granularity` + which suite pages mount `PracticeSuiteChrome` (O1 evidence).
3. `practice-campaign-select` vs `blotter-campaign-filter` (two campaign controls today).
4. Open:N chip + `filterOpenOnly` (file + line).
5. Badge `onCampaignFilter` + `?campaign=` deep link.
6. Playbook select on blotter (out of slice — inventory only).
7. `journey-adhere-locate-banner`.
8. `web/e2e/trade-log-find.spec.ts` — must stay green after extract.

**Done when:** `agents/p-autofilter/reviews/TLAF0-4-kilo.md` with command + output. No code.
