# TLAF1-1 — Charlie extract shared Autofilter

**GO TLAF1** (Coach). INTERNAL — not a deploy.

**In:** `web/lib/autofilter/**` · `web/components/autofilter/**` · Find and Badge consumer `TradeFindTag.tsx` · re-exports from old trade-log paths.

**Out:** Trade Log title bar · nav/button removal · TLAF2 · Open:N · playbook select.

**Done:** Shared module exists; Find and Badge imports it; e2e testids unchanged (`autofilter-*`, `campaign-autofilter-toggle`).
