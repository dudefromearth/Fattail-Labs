# TLAF1-2 — Kilo Autofilter extract tests

**GO TLAF1.** `npx --yes tsx lib/autofilter/apply.test.ts` (A7/A8/A9/A12). Grep: no `function AutoFilter` in Find and Badge; implementation under `web/components/autofilter/`. No persist/`lastUsed` in `web/lib/autofilter/apply.ts`.
