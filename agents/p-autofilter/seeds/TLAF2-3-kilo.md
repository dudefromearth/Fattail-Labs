# TLAF2-3 — Kilo — A1–A12 + suite chrome

**GO TLAF2.**

**Does:**
1. Trade Log A1–A12 as applicable (A10 N/A — Open:N removed).
2. Find and Badge extract still the shared menus (`web/lib/autofilter`, `web/components/autofilter`).
3. Out-of-scope smoke: Journal, Reports, Retro, Playbook each still render `practice-granularity` and `practice-campaign-select`.

**Files:** `web/lib/tradeLogAutofilter.test.ts` · `web/lib/tradeLogAutofilter.tlf2.test.ts` · `web/e2e/trade-log-autofilter.spec.ts` · `web/lib/autofilter/apply.test.ts`.
