# TLAF3-2 — Kilo — help catalog + concierge headings

**GO TLAF3.**

**Does:**
1. `GET /api/help/guides` includes `trade-log-autofilter`; `GET /api/help/guides/trade-log-autofilter` 200 published.
2. Concierge headings for the new topic + search hits (title bar, four columns, Filter on, badge).
3. Existing Find and Badge Autofilter search still hits.

**Files:** `server/tests/test_help_catalog.py` · `server/tests/test_help_ai.py`.
