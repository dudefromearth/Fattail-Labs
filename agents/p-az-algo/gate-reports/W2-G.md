# W2-G — Builder Algo

**Verdict:** **PASS** (code landed; Echo HIG deferred to W-G per DL-479)

- Type Algo Save **on** when `algoEligible` (OTM debit fly).  
- `alert_class: algo`, `trigger.family: algo`.  
- **+** `data-algo-pulse=1` + pulse class when eligible and no live algo.  
- Click **+** seeds Type Algo. Holder subtitle **Waiting**.  
- **No** `HostPnLChart.tsx` in this packet.

Smoke (Coach): eligible fly → + pulses → Builder Algo description → Save → holder Waiting.
