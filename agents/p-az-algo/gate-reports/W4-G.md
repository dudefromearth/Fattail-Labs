# W4-G — Algo canvas trail

**Verdict:** **PASS**

Paint-only on `HostPnLChart.tsx`: dashed verticals at `x_H` / `x_S` (already wired via `algoTrailLines`); overlay band between them when overlay is on; pulse raises band alpha. Host `data-algo-phase` / `data-algo-side`. Waiting draws no trail lines. Invert-missing: no invented `x_S`. Pan unchanged (`bindChartHost`). Menu rows ≥44pt (H9, shared host).

W4-0 **APPROVE paint-only** after Packet A **W-G PASS**.
