# Seed W0-2 — India parents / lock

**Project:** p-az-algo  
**Agent:** India  
**Phase:** W0  
**Depends:** W0-1  
**Law:** AZ-ALGO v1.0.1 · OT-EF · AZ-ALB §2.6 · plan FP12  
**Gate it feeds:** W0-3…6 · W0-G

## Ask

**APPROVED** or **RETURNED** (build readiness). Confirm the v1.0.1 fold is architecture-safe.

Must say, first paragraph:

1. ALGO-B1 is **one re-inverting trail**, not two lines, not silence on the far wing.  
2. ALGO-A1 monotone `f` is required (give-back of protection).  
3. Invert-missing is OT-EF named state + P&L backstop **only then**.  
4. **W4 file lock:** `HostPnLChart.tsx` **draw only**. Do not treat W4 as C2 GO.  
5. Demo (FI-033) is **not** this program.

**ALGP-B1 — quote three neighbor artifacts** (from **their** `gate-reports/` or ORCHESTRATOR status row, path + excerpt — not this plan’s parent table):

| Neighbor | Quote |
|----------|--------|
| `p-alerts` | **C1-G** (W2 edits `AlertBuilderDialog.tsx` + adapter) **and** **C2** status |
| `p-az-viewport-2d` | **W-G** (or current packet if W-G unfiled) |
| `p-az-viewport-return` | **current packet** — name **`OpfRiskAnalyzer.tsx` overlap** if that board holds scope there (this board’s W2/W3/W4 all wire through it) |

A table assertion without those quotes is **RETURNED**.

## Out

Rewriting Coach §0. Moving OPF τ. Inventing Manager HTTP schema.

## Invariants

DL-309. No MSC. No second market WS. Coach Content Law: block only invariant breakage.

## Done when

`gate-reports/W0-2-india.md` with verdict, § Bench delta, § Flagged ideas (inventory intact or named).

## Next if APPROVED

Echo · Tango · Hotel · Victor in parallel.
