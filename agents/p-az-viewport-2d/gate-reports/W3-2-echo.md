# W3-2 — Echo Packet A cursor / hit

**Verdict:** **APPROVED** (as-built)

Plot cursor is `grab` / `grabbing` (`bindChartHost` + host hover). Hit on a position tent uses `pointer` while idle-hover; primary tent interaction remains grab-to-pan. No new chrome. Axis pads unchanged. HI invariant intact.
