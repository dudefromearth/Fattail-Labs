# Seed W0-M — Kilo FAIL the splitter

**Project:** p-az-viewport-return  
**Agent:** Kilo  
**Depends:** W0-0 (may run ∥ W0 review)  
**Gate:** W0-G · W0-BA

## Ask

Produce a **FAIL** that matches:

- Hard refresh: drag **and** wheel work.  
- Leave and return (**no** hard refresh): position visible; drag **and** wheel do **not** work.

**RH-A2:** Run **Coach’s path first** (W0-0: analysis §3 A–F, Coach’s order). Then remaining permutations. Do not guess among tab-within-app, suite route, browser tab, Back, minimize — those are different lifecycles.

## Rules

- Real pointer + real wheel. `dispatchEvent` on the host is **not** the sole proof.  
- Listed Create 20-wide at spot **or** Coach’s book — not only the 5990 unlisted seed.  
- Dump JSON (hit stack, `inert`, `data-wheel-bound`, ticks, `viewX`).  
- **PASS is illegal** on this gate.

## Out

Product code. Declaring a cause without a FAIL.

## Done when

`gate-reports/W0-M-kilo.md`: **FAIL** (reproduced on Coach’s path or a permutation after it) or **BLOCKED** (Coach’s path / all A–F green and Coach still fails — Coach at the machine). Evidence JSON cited. **PASS is illegal.**
