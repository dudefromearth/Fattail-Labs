# IF-4 allowlist vs this packet

Plan allowlist: Factory domain, admin Factory UI, member pill, Factory
routes, `migrations/NNN_iki_factory*.sql`, Factory tests, board docs/DL.
**Never:** `web/lib/runner/**` · `gemba.md` · AppChrome · BoardKanban ·
wiki git · `content_items`.

## In allowlist (untracked or modified this program)

Factory domain + Woo seam, live route, migrations 137–142, board + Live
catalog, IF-1…IF-5 tests, `agents/p-iki-factory/`, GO tokens IF-1…IF-5,
plan v1.1, DL-577/578, Arch README + wiki-design signal sentence,
`.env.example` stub note.

## Forbidden trees

This packet did **not** edit `web/lib/runner/**`, `agents/bench/gemba.md`,
or `BoardKanban.tsx`. `AppChrome.tsx` and runner files are **pre-existing
dirty** from other programs (session start git status). Not IF-4/IF-5.
