# IF-1-3 — Board HIG + copy (Echo + Tango)

**GO IF-1.**

## In scope

New Factory board component (do **not** edit `BoardKanban.tsx`).  
Five lanes. Drag-and-drop + click-to-advance/detract. Hit target ≥ `--hit-min` (44px). HI tokens (`--color-label*`). Priority chips Low/Medium/High. Hold control obvious.  
Tango: empty Ideas and Hold copy = workshop, not oracle. No profit claims. Pickup “waiting for skills” is honest (runner not live).

## Out of scope

Member pill restyle. Spec Approve button (forbidden — plan O6).

## Completion

Charlie consumes this grammar. Copy has no P&L / profit claim.  
