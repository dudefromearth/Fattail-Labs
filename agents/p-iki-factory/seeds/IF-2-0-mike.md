# IF-2-0 — Principal `gemba` (Mike)

**GO IF-2.**

Seed `agent_principals` callsign `gemba`, display Lean Factory Worker, status active. Scope `factory:operate` when a key is minted. **No** `contracts:deliver`.

## Out of scope

Wiki envelope. Admin cookie as Gemba.

## Completion

`SELECT callsign FROM agent_principals WHERE callsign='gemba'` → one active row.
