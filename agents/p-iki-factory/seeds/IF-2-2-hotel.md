# IF-2-2 — Finding shape (Hotel)

**GO IF-2.** Does **not** author a skill.

Frozen output shape: `title`, `rank` (int), `reason` (why this rank, evidence — not advice), `sources` (list of refs).  
Forbidden: profit claims, fills, “you should”, expected return.

## Completion

`agents/p-iki-factory/hotel-research-finding-shape.md` exists. Domain rejects findings that fail the shape.
