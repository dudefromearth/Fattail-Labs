# IF-2-3 — Research chrome (Charlie)

**GO IF-2.**

On the suite Factory board: blocked reason (empty registry / window expiry); rank + reason + sources on child cards; remainder on the parent Idea. Do not invent copy. No Help Package chrome.

## Completion

Blocked Idea is visible without a fake set of 10 cards.
