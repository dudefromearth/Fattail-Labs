# IF-3-0 — Spec draft + conveyor (Alpha)

**GO IF-3.**

On Admin Research→Spec: compose `spec_md` from proposal + sources + Admin notes (no invention); `spec_ready`; waiting-for-plan if no `plan_ref`.  
`plan_ref` attach = Spec approval. If Spec-ready + plan + not Hold → auto-advance to Build, `built_ready`, visible auto-move reason. Hold skips; clear Hold resumes. Rework destination Admin-only.

## Out of scope

Deploy/Live. Wiki. Named skills.

## Completion

Kilo: no auto R→S; conveyor only with both inputs; Hold skip/resume; Gemba cannot Rework.
