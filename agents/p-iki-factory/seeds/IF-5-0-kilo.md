# IF-5-0 — Hardening characterization (Kilo)

**GO IF-5.** Feeds IF-5-G.

`server/tests/test_iki_factory_if5.py` titles `zz-if5-*`.

Lineage: Idea→Research→Spec→Build→Live on transitions.  
Notify: `notify_admins` called on Published; Deploy still succeeds if notify raises.  
Invalid-move matrix: skip-forward, agent R→S, Gemba backward.  
Hold skip/resume (Spec→Build and Build→Published).  
Auto-move reason present on every auto transition.  
Empty registry fail-loud. Window expiry visible. Missing product spec stays Build.  
Woo stub: Published + named reason, never Build.  
Hotel grep: no profit/advice in Factory agent-drafted strings.  
Zero wiki / runner writes.

Pytest IF-1…IF-5 green.
