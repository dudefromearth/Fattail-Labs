# Seed IF3-3 — Kilo Journal characterization

**Agent:** Kilo  
**Depends on:** IF3-1  

Layout/session tests that already exist stay green. Add bind/display tests Echo names. Isolation journal tests still pass (A never sees B).
