# Seeds — p-journal-session-v04

Pasteable work packets. Each seed must be executable from a cold session.

## Format (mandatory)

- Project name + agent callsign + task sequence  
- Spec citation (v0.4a section)  
- Files in scope / out of scope  
- Invariants  
- Completion criteria (verifiable)  
- Reviewers + gate it feeds  

## Order

Write **JS0-*** first (locks + GO). Do not write implementation seeds that assume  
BUILD AUTHORITY until **JS0-0 Coach GO**.

Indicative IDs: see [`../IMPLEMENTATION-PLAN.md`](../IMPLEMENTATION-PLAN.md).

## Supersession

Do not use seeds from `agents/p-journal-session/seeds/` for this program — they  
encode the v0.2 form-primary frame.
