# Seeds — p-journal-session-v05

**J0 review/GO seeds first.** Implementation seeds only after **JS5-R0 Coach GO**.

Do not copy seeds from `p-journal-session` or `p-journal-session-v04` — wrong product frame.

J4 seeds must declare dependency on Tag Manager TM2 PASS.
