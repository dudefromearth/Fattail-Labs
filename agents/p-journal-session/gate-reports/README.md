# Gate reports — p-journal-session

Delta writes one report per phase end: **PASS / FAIL / BLOCKED** with command+output evidence.

Naming: `JS{n}-G-*.md` (e.g. `JS1-G-schema.md`, `JS9-G-program-close.md`).

Delta does **not** modify work under review.
