# W0-12 — Picker Spec v1.0.2 + route reconciliation

**Agents:** Juliet · Lima  
**Phase:** W0  
**Depends on:** none (can parallel other W0 gate writeups)

## Intent

Two gates (C1-1 / AT-MB5, and Phase C route rebuild) need a **real artifact**, not an assumed revision number.

## Deliverables

1. **Canonical file:** `Specs/FatTail-Labs-Options-Chain-Picker-Spec-v1.0.2.md` containing:
   - **OC6a** (exact listed strikes / cent-exact display)
   - **OC15** stages (H1-2 minimal → MB-P1 successor)
   - **As-built routes:** primary `/app/options-lab`; legacy `/app/market/chain-ladder` → redirect
   - **OD-nav:** product catalog name still open (Coach · Echo); Spec uses “chain ladder surface”
2. **Stub** at `…-Spec-v1.0.md` pointing to v1.0.2 (do not leave dual full copies to diverge).
3. **Link sweep:** bus Spec, bus bench plan, picker bench plan, `agents/p-options-chain-picker/*`, `agents/README.md` → `v1.0.2` path.
4. Gate report note: AT-MB5 cites **OC6a** on **v1.0.2** filename.

## Exit

File on disk; links grepped clean of stale “v1.0.2 content lives only in v1.0.md”; report `gate-reports/W0-12-picker-v102-routes.md`.
