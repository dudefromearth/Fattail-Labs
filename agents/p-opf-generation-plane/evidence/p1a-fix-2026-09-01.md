# P1a-FIX — 2026-09-01

**Host:** StudioTwo. **Scope:** infra only. P1a-G PASS `60ddc0d` not reopened.  
**Not AT-GP23.** P1b does not start.

No secret values in this file.

---

## Pass 1 — live API regression check

Pid **90357** still listening on `127.0.0.1:4001`. `/api/health` HTTP 200 (`env=dev`).

### Env NAMES on 90357

`POLYGON_API_KEY` **present**. `HEYGEN_API_KEY` **present**. Also present: `LABS_MARKET_BUS`, `REDIS_URL`, `LABS_OPF_STORE_MAX_STALE_MS`.

Repo `.env` **before this packet:** both Massive/HeyGen names **missing** (A′(4) restart hazard). Live process kept them because Pass C overlay-sourced `.env` in a shell that already had them.

### Functional fetch (not an env check)

`GET /api/auth/dev-login` → cookie name `ft_session` (value not recorded).

`GET /api/me/market/chain-ladder/expirations?symbol=SPX` → HTTP 200, `default_expiration=2026-09-01`.

`GET /api/me/market/chain-ladder?symbol=SPX&expiration=2026-09-01&side=call&wings=25` → HTTP 200, `content_hash` present, `row_count=102`, `n_rows=102`, `product=SPX`, `spot` present. **FETCH_OK.**

**Verdict:** nothing was lost on the live API. Fetch returns data. API **not** restarted.

**Durable restore (names only):** appended `POLYGON_API_KEY` and `HEYGEN_API_KEY` to the repo `.env` so `set -a && source ../.env` carries them. `.env` mode 600. Not committed.

---

## Pass 2 — chain-feed skip

### Quoted exception (actual log, 403 identical ticks)

```
skip mb:ladder:I:SPX:2026-09-01:w25:dual: Missing required environment variable: LABS_ENV
```

`/tmp/labs-chain-feed.err` was empty (0 bytes). No `wrote` and no `fail` lines before the reload.

### Chain from evidence

Coach's chain is **confirmed**, with one observation note:

1. Plist `EnvironmentVariables` supplied only `LABS_MARKET_BUS` and `REDIS_URL`. Observed on `launchctl print` (P1a).
2. `chain_feed.py:65–67` `skip {topic}: {exc}` wraps `cl._resolve_universe_symbol`. That path hits `db.transaction()` → `get_config()`. **Observed exception is `LABS_ENV`**, which is `_require` first in `config.py:63`. Fail-loud stops there — we never saw `LABS_DB_*` in the log.
3. `config.py` fail-loud set is larger than eleven names today (playbook, rate limits, …). The **first** required name is `LABS_ENV`. Coach's “eleven … LABS_ENV first” matches the observed skip.
4. `_fetch_ladder_uncached` (`chain_feed.py:70`, `fail {topic}` at line 93) was **not reached** while `LABS_ENV` was missing. After the fix it is reached (see `wrote` below). It needs `MASSIVE_API_KEY` or `POLYGON_API_KEY` (`massive_client.py:43–47`). That name was not in `.env` until Pass 1's durable append.

### Fix

- `infra/launchd/run-from-repo-env.sh` — `source` repo `.env`, exec venv python. No secrets in the file.
- `infra/launchd/ai.fattail.labs.chain-feed.plist.example` — `ProgramArguments` is that wrapper. `EnvironmentVariables` still only the two non-secret bus names.
- Reloaded `~/Library/LaunchAgents/ai.fattail.labs.chain-feed.plist`. SSR still `Could not find service`. API pid 90357 stayed up.

### Past the skip (after reload)

Pid 99058, `state=running`, never exited. New log:

```
wrote mb:ladder:I:SPX:2026-09-01:w25:dual hash=73e80b886b7ff3a1 rows=102 dual=True
wrote mb:ladder:I:SPX:2026-09-01:w25:dual hash=f190adf7a519b669 rows=102 dual=True
wrote mb:ladder:I:SPX:2026-09-01:w25:dual hash=f6647708b9625c74 rows=102 dual=True
wrote mb:ladder:I:SPX:2026-09-01:w25:dual hash=dc0c67756e00dfec rows=102 dual=True
wrote mb:ladder:I:SPX:2026-09-01:w25:dual hash=0af60cb81e2e28d3 rows=102 dual=True
```

stderr empty. SCAN `mb:ladder:*` → `mb:ladder:I:SPX:2026-09-01:w25:dual`.

**Not AT-GP23.** A member Options Lab tab holds that I:SPX w25 interest. This packet only proves the feed can resolve and fetch when interest exists.

---

## AT-GP23 precondition (for P1b)

A member Options Lab tab on StudioTwo holds I:SPX w25. AT-GP23 must prove the **plane** holds interest, not a member. Running it while a member tab is open is invalid for the same reason SSR live-capture must stay unloaded (GP21). P1b needs a quiet window with no member watching, **verified before the test**. Recorded on `seeds/P1b-2-kilo.md` and `seeds/P1b-1-alpha-interest.md`.
