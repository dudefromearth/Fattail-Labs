# P1b AT-GP23 + suite attribution — 2026-09-01

**Host:** StudioTwo. **API pid 90357 not restarted.** Next pid 75151 not killed.  
**Law:** spec v0.2.2 + plan as folded at `374ed86`. Coach disposition: own process, not `main.py`.

No secret values in this file.

---

## 1. Suite attribution (Invariant 10)

Same command: `cd server && .venv/bin/python -m pytest tests -q`.

| Tree | Result |
|------|--------|
| Working tree (P1b files) | 47 failed, 1391 passed, 5 skipped, 956 s |
| Parent `3daf090` (clean worktree `/tmp/p1b-parent-3daf090`) | 46 failed, 1380 passed, 5 skipped, 1015 s |

HEAD has 12 extra passes = `tests/test_opf_plane_interest.py` (12/12 in isolation).

**Intersection: 46 failures, identical nodeids.**

**HEAD only:** `tests/test_opf_session_envelope.py::test_cl21_ladder_http_carries_opf_session` (`open` vs `extended`). Re-run on the working tree: **1 passed in 0.15 s**. Live session flake, not this change.

**Parent only:** none.

**Invariant 10 for this change: satisfied.** No durable HEAD-only failure.

### Pre-existing red suite on main (named finding)

These 46 fail at `3daf090` and at HEAD. Environment / schema / fixture, not P1b:

| Cluster | n | Kind |
|---------|---|------|
| `test_ai_admin_api.py::test_ai_run_bravo_live_via_api` | 1 | **Live/AI.** xAI HTTP 403 team credits / monthly spend limit. |
| `test_iki_factory_approval_price.py` + `test_iki_factory_if{1,3,4,5,6,8}.py` | 36 | **Schema.** `Unknown column 'price_cents' in 'field list'`. Dev DB behind the tests. |
| `test_journal_sessions.py::test_member_context_pack_covers_journey_book_journal_retro` | 1 | Copy assertion (`sized half if unsure` missing). |
| `test_progress_db_contract.py::TestRefreshUsesCursor::test_prune_uses_a_cursor` | 1 | `datetime.utcnow()` DeprecationWarning as error. |
| `test_strategy_lab_curate.py` (7 tests) | 7 | **Fixture.** `Phase 'development' is full (max 100).` HTTP 409. |

A red suite on main is its own defect. P1b does not inherit it silently and does not fix it.

---

## 2. What refreshes w25 member interest (observe only)

| Fact | Evidence |
|------|----------|
| next-server pid 75151 | `next-server (v16.2.10)`, elapsed **7d+**, listen `*:3001` |
| WS to API | `75151 → 127.0.0.1:4001` ESTABLISHED (stale) |
| Who is on 3001 | **sshd-sess 74602** (SSH forward), not Chrome/Safari/Arc |
| Local browser on 3000/4001 | none |
| w25 interest at P1a-FIX | present, no sidecar |
| w25 at this packet | **TTL -2 / EXISTS 0** |
| 60 s watch (16:07:12–16:08:03) | `ttl_w25=-2 exists=0 keys=[]` every 10 s — **does not reset** |

**Verdict:** the 7-day WebSocket is **next-server + SSH tunnel**, not a live heatmap heartbeat. Interest is **not** being refreshed by pid 75151. Tab-closing would not have been the lever anyway: the socket outlived the interest key. Observed only. Next not killed.

---

## 3. AT-GP23 — topic isolation (w10)

**Execution note:** same underlier/expiration, wings **w10** vs member **w25**. Global quiet window not required.

### a. BEFORE load

```
SCAN mb:interest:*     (empty)
SCAN mb:interest_src:* (empty)
EXISTS w10 = 0
EXISTS w25 = 0
EXISTS w25 sidecar = 0
SSR: Could not find service "ai.fattail.labs.ssr-live-capture"
```

w25 member topic was **already expired** (see §2). Control becomes: plane must not **create** w25 or a w25 sidecar.

`LABS_OPF_PLANE_WINGS_TOPICS=I:SPX,2026-09-01,10` (not a secret). `LABS_OPF_LISTED_PAIRS` unset.

### b. Load

`~/Library/LaunchAgents/ai.fattail.labs.plane-interest.plist` from the example. `run-from-repo-env.sh -m opf.plane_interest`. SSR unloaded before and after.

First load: log `api_down skip tick` (health URL `:4000` = `LABS_PORT`; live API is `:4001`). API pid 90357 stayed up. Item 9 live.

Then `LABS_PLANE_INTEREST_HEALTH_URL=http://127.0.0.1:4001/api/health`. Reload **plane-interest only**. API not restarted.

### c. AFTER

```
touch mb:ladder:I:SPX:2026-09-01:w10:dual src=plane
GET mb:interest_src:mb:ladder:I:SPX:2026-09-01:w10:dual → plane
list_interest_topics("mb:ladder:") → ['mb:ladder:I:SPX:2026-09-01:w10:dual']
w10_unmodified True
EXISTS w25 = 0
EXISTS w25 sidecar = 0
```

Plane added w10. Did not invent w25. GP21.

### d. chain_feed + mb:pub

```
wrote mb:ladder:I:SPX:2026-09-01:w10:dual hash=e3a2336702a29454 rows=42 dual=True
```

`PSUBSCRIBE mb:pub` caught:

```
{"topic": "mb:ladder:I:SPX:2026-09-01:w10:dual", "hash": "e3a2336702a29454", ...}
```

Idle-clause is not evidence while any other topic could exist; the w10 `wrote` + `mb:pub` is the positive proof.

### e. Heartbeat < 45 s grace

TTL of w10 interest over 50 s wall (no member on w10):

```
16:11:12 ttl_w10=37 exists_w10=1 exists_w25=0
16:11:22 ttl_w10=43
16:11:32 ttl_w10=33
16:11:42 ttl_w10=38
16:11:52 ttl_w10=43
16:12:02 ttl_w10=33
```

TTL resets (heartbeat 15 s). Key survives past 45 s wall. w25 stays absent.

### f. Item 9

Live: skip ticks when health URL is not 200 (`:4000`), API on `:4001` never taken down. Unit: `test_tick_skips_when_api_down`.

---

## Boundary

Five frozen modules not edited. SSR not loaded. No `--workers`. MiniTwo not touched.
