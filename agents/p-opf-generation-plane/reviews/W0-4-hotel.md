# W0-4 — Hotel book honesty

**Agent:** Hotel  
**HEAD:** `374ed86`  
**Date:** 2026-09-01  
**Out honored:** no product code · no `gex_v2` · Heatmap tree not edited (DL-539 1/3)

**Verdict:** **APPROVED.** GP7 is law for the plane. Existing Heatmap chrome already says “Chain GEX (estimate)” on a **window** — that is **out of this GO** and must not be used as a license for the plane to emit the same label.

---

## Confirm

| Law | Reading |
|-----|---------|
| **GP7** | A `wings` generation is a **window**. Lawful when labelled `book: "wings"`. It is **not** chain GEX. Payload, chrome, tooltip, export, print template — none may say chain GEX for a window. |
| **Listed completeness** | GP16–GP17: no clamp; page until complete; truncation **fails loud and writes nothing**. `max_pages=3` on the live ladder (`chain_ladder.py:308–309`) is HM18 coincidence, **not** the listed bound. |
| **`w0` banned** | A magic number where a token belongs. Listed key is `…:listed:dual` (GP18). |
| **Measured contrast** | GXA0: `wings=15` → 62 rows; full listed 0DTE → 496 contracts / 248 strikes. Teaching contrast, not a session oracle. |
| **As-built Heatmap** | `gex.ts:184–188` label **“Chain GEX (estimate)”** on the dual-side **wing** ladder. Honest as a Heatmap *estimate*; **forbidden as plane/analytics copy** (GP7). Do not “fix” Heatmap in this program (allowlist / DL-539). |

Hotel blocks any later seed that would ship window GEX as chain GEX, or write a partial listed book.
