# Seed W0-2 — India spec / architecture

**Project:** OPF Generation Plane  
**Agent:** India  
**Depends:** W0-0  
**Law:** GP1–GP24 · Arch 28 · Arch 30 · GXA0  
**Out:** product code · inventing L4-A v0.4 · lifting GEX freeze

## Ask

Quote the tree (path + line). Confirm or return:

- F8 is closed by **design** (hydrator + namespace), not by hoping `_hydrate` reads Redis.
- `owned` / `supplied` is a **key** rule, not a filter.
- Analytics-class helper refuses `supplied` (AT-GP2 bound to helper, no analytics URL).
- Listed key `…:listed:dual` and `parse_ladder_topic` end-anchored (`I:SPX`).
- Three clocks; refcount is not warmth.
- Hydrator in-process (GP11); GP23 workers=1.
- GP1a: pricing POST path preserved.
- Arch 30 §10 hours-TTL, §11 multi-worker SoR, §4 MiniTwo topology, §6.1 pagination, §17b 19 ATs — honesty list is P0, not silent.
- L4-A remains frozen. Consumer cite v0.4 is **missing from tree** — name it, do not write it.
- `docs/OPF-REFERENCE-v1_1.md` missing — name it.

APPROVED or RETURNED for build readiness. Coach Content Law.

## Done when

Review in `agents/p-opf-generation-plane/reviews/W0-2-india.md`.
