# B-G — Builder defaults

**Status:** **PASS** (code)  

- Empty create → `butterfly` on ATM listed strikes (`regenerate("butterfly", …)` + `setTemplate("butterfly")`).  
- Wing width from `profile.fly_widths[0]` (A2/A3) with listed snap.  
- Listed-only via `snapToListed` / `listedWingChoices`.
