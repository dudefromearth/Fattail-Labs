# D-G — Domain hygiene (PB v0.3)

**Status:** **PASS**  

- Create path `status: "ANALYSIS"` only; migrate forces ANALYSIS.  
- OMS tokens reserved type only.  
- `packageMagnitudeInvariantHolds` + dev assert on unlock apply path.  
- Six-state `LiveState` unchanged.
