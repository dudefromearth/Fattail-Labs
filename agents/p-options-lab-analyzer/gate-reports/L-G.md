# L-G — Layout residual

**Status:** **PASS**  
**Evidence:** `OpfRiskAnalyzer.tsx` restructured:

1. `data-testid="analyzer-controls-strip"` — top compact Controls  
2. `data-testid="analyzer-viewport-region"` — Risk | Surface  
3. `data-testid="analyzer-book-divider"` — separator  
4. `data-testid="analyzer-positions-region"` — Positions under viewport  
5. `data-testid="analyzer-alerts-region"` — Alerts under list  

No left-rail book. Session book/alerts state preserved (same sessionStorage).
