# S-G — Stream / stale + posture fixtures

**Status:** **PASS** (with fixture unit evidence)

- Cache paint label: `· cache stale` when `risk.fromCache && !loading`.  
- Posture plane mapping: `web/lib/options-lab/sessionPosture.ts` + fixtures.  
- Server: `pytest tests/test_market_session_posture.py` → **6 passed** (closed / early-close / extended-hours not open).  
- Full UI holiday fixture in browser deferred to K if needed; plane law covered.
