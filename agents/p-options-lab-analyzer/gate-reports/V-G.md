# V-G — VP bins-only

**Status:** **PASS**  

- `VolumeProfileChart` renders `data-testid="volume-profile-bins-host"`.  
- `data-testid="volume-profile-no-candles"` / no `addCandlestickSeries`.  
- OHLC store retained (`ohlcBars` on SeriesEntry) as bin input.  
- POC / VA labels on surface.
