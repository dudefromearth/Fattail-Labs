# Seed W0-1 — Lima Spec hash + residual DL

**Agent:** Lima  
**Phase:** W0  

## Ask

1. Recompute Analyzer Spec content hash (exclude integrity line); update header if drift.  
2. Confirm DL-301…304 cite residual plan path.  
3. File residual program open note if missing (plan v1.0 · board path).  
4. Index plan in Architecture README / AGENTS if needed.

## Done

Hash matches body; docs index points to plan + board.
