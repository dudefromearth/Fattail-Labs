# Orchestrator — Options Lab Heatmap GEX Calendar

**Juliet** runs this board **after** BUILD AUTHORITY. Specialists only via seeds. Gates via **Delta** ternary. Spec/gate reviews → **GROK ADVISOR via Coach**.

**Instance:** **GROK BUILD — HEATMAP (GBH)**  
**Spec:** v0.1 **DRAFT** · sha1 `36a347185aaece5c4b900bf22e9683ad4ffbaf6f`  
**W0 token:** [`agents/go/GBH-W0.md`](../go/GBH-W0.md) **HOLD**

### Critical path

```text
HOLD (now) → OD-GC1…GC4 stamp on GBH-W0
          → India / Echo+Tango / Hotel / Lima DL
          → GC0 BUILD AUTHORITY
          → GC1 flag+fixtures → GC2 compute → GC3 chrome → GC4 switcher+help
```

| Phase | Name | State |
|-------|------|--------|
| **HOLD** | Seating · DRAFT spec · OD-GC1…GC4 open | **NOW** |
| **GC0** | Spec GO · OD stamp · reviews · DL | Blocked on Coach stamp |
| **GC1** | Pack type + AT-GC1 / AT-GC8 / AT-GC9 on fixtures. Flag on. No switcher | Blocked on GC0 |
| **GC2** | Pure `gexCal.ts`: cells, NET, profile, peak | Blocked on GC1 |
| **GC3** | `matrix-profile` chrome | Blocked on GC2 |
| **GC4** | Switcher (after ODs + GC1–GC3 green). Help after the view works | Blocked on GC3 |

**Data law:** Frozen `gex_v1` (`pricing.ts`). Dual-side books. **No volume. No second Massive. No MSC. No fake columns.**

### Gate protocol

1. Seeds executed → evidence in `gate-reports/` or cited paths.  
2. **Delta** phase gate: PASS / FAIL / BLOCKED. Reviews to ADVISOR via Coach.  
3. No SKIP. Overrule of a specialist finding = **DL entry**, not silent waive.  
4. Conflict or missing dependency: **STOP and report to Coach.**

### Seed naming (do not author until GC0)

`GC1-{n}-{agent}-{slug}.md` · … · `GC4-{n}-…`  
Gates: `GC1-G.md` · … · `GC4-G.md`

### Named holds (not surprises)

| Item | Status |
|------|--------|
| OD-GC1…GC4 | Unstamped — HOLD by design |
| `ChainContext` is one expiry | Spec GC1–GC3; not a build block while HOLD |
| `HeatmapChainPanel` / `OptionsLabChrome` / `heatmap/page.tsx` | Shared with LIM / WF / AF — **Coach before edit**; prefer `HeatmapGexCalendar.tsx` sibling |
| ME Spec id `calendar` | Different template. Do not fuse with `gex-cal` |
| LIM active on the same route | Do not edit LIM files |

### Do not

- Treat DRAFT or this board as GO.  
- Seed from the spec until Phase 5 + OD stamp.  
- Ship a production switcher entry.  
- Rewrite `gex_v1`. Restyle frozen `gex` or LIM.  
- Edit VPS* / VPSB* / SADEV* / VP / volume-profile.  
- Fork a Time Machine scrubber.  
- `git add -A`. Stop StudioTwo `:3000` / `:4000`. MiniTwo.
