# Orchestrator — Heatmap GEX Calendar (GBH)

**Instance:** **GROK BUILD — HEATMAP (GBH)** (**DL-747**). StudioTwo. Never `git add -A`. Do not stop `:3000` / `:4000`.

**Spec:** [`Specs/FatTail-Labs-Options-Lab-Heatmap-GEX-Calendar-Spec-v0_1.md`](../../Specs/FatTail-Labs-Options-Lab-Heatmap-GEX-Calendar-Spec-v0_1.md) sha1 `36a347185aaece5c4b900bf22e9683ad4ffbaf6f` · **DRAFT** until **OD-GC1…GC4**.

**Namespace:** `HM*` / `OD-GC*`. Data domain: frozen **`gex_v1` generations**.

### Does not execute

`VPS*` / `VPSB*` / `SADEV*`. VP stores. VP surfaces. SA heatmap-overlay / SA-edges consumer.

### Disambiguation

gex-cal Heatmap template (**this board**) is **not** the VP program's future "heatmap overlay." Distinct products. Separate Labs route trees: heatmap vs volume-profile.

### Contention

Collectors win. Shared Labs components change only through Coach. Cross-instance conflict → **STOP, report to Coach**.

### Status

| Item | State |
|------|--------|
| Spec v0.1 | DRAFT · sha1 `36a34718…` |
| OD-GC1…GC4 | **unstamped** — no BUILD |
| Token | none until Coach GO |

### Do not

- Stamp ODs in chat  
- Touch VP files  
- Fuse with LIM / frozen `gex` profile / ME `calendar` structure  
- Negotiate with INFRA or APPS
