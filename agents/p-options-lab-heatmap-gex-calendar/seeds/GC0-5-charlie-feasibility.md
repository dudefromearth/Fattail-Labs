# Seed GC0-5 — Charlie feasibility (read-only)

**Agent:** Charlie  
**Out:** no `gexCal.ts` in this seed.

Feasible: N `setChainInterest` ids on existing `MarketSocket` (India GC0-1). Local `GexCalPack`. Pure `gexCal.ts` calling `gexSide`/`gexNet`/`gexAbs` only. Sibling `HeatmapGexCalendar.tsx`. Host branch `layout === "matrix-profile"` only. Flag `LABS_HEATMAP_TERM_MASS` (JR2). Registry **append**. Do not edit `gex.ts` compute.

**Done when:** this note; GC1 may fire after GC0-0.
