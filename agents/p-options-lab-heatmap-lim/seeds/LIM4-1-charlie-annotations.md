# LIM4-1 — Annotations hook

**Project:** Options Lab Heatmap LIM  
**Agent:** Charlie  
**Depends:** LIM4-0  
**Feeds:** LIM4-G

## In scope

Optional hairline at `spot + centrePts` and interval ticks at each `lo`/`hi`. Gated by `LIM_SHOW_ANNOTATIONS` default **false**.

## Law

LIM30–32. Never spot glow + COG + ticks all on by default. Compact: none of them.

## LIM4-1 done

Default off on a fixture. Flag-on fixture shows ticks as intervals, not midpoints.
