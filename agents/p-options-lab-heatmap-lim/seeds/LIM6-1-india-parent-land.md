# LIM6-1 — Parent amendment lands

**Project:** Options Lab Heatmap LIM  
**Agent:** India  
**Depends:** LIM5-G · LIM0-1 draft  
**Feeds:** LIM6-G

## In scope

Heatmap Templates Spec bump (OD-LIM6). LIM Spec changelog if as-built drifted. Hash vs GO.

## Out of scope

Implementing `session-volume`. Changing HM21 SVP clauses except to keep them.

## Work

1. Land `layout: "quadrant"` + catalog `lim` on the parent. Preserve SVP auxiliary-plane clauses. One PR.  
2. If LIM as-built drifted, changelog beside Coach text — do not silently drop Coach content.  
3. sha1 matches GO or a new DL records the new hash.

## LIM6-1 done

Parent file on disk is the merged amendment. No second parallel Templates spec.
