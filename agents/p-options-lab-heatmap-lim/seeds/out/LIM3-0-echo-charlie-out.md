# LIM3-0 — Quadrant plane (out)

**Agents:** Echo (IA lock) · Charlie (render)  
**Law:** Spec v0.4.3 §7.3 · Echo IA LIM0-3  
**Date:** 2026-09-02

`HeatmapLimQuadrant.tsx` maps `LimResult` only. Dot opacity is `LIM_DOT_OPACITY = 1`. Ring radius scales with `1 − crossingProximity`. Compact keeps the ring. Empty / never-hydrated / `valid: false` sit at (0, 50). Identity `#0a84ff`. Density control is kit `SegmentedControl` (≥44pt). No cell names. No gex.ts.
