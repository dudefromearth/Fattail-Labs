# LIM4-0 — Spot-line glow (out)

**Agents:** Charlie · Echo  
**Date:** 2026-09-02

`gexSpotGlowCss` is opt-in. Frozen GEX never passes a link → `null` (no class, no style). LIM companion draws a continuous spot **line** with `--lim-identity` glow. Bars do not glow. Frozen `heatmap-gex-profile` JSX unchanged; SHA1 matches e1c1ef1.
