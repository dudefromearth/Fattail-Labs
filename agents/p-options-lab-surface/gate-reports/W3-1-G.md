# W3-1-G — Host + Law B + exact/locked tent

**Verdict:** **PASS**  
**Date:** 2026-08-16  
**Agents:** Charlie · Alpha  
**Depends:** W0-G · W1-G · W2-G  

---

## Files touched (seed only)

| Who | Path |
|-----|------|
| Alpha | `web/lib/risk-graph/surfaceModel.ts` (τ-window + T-GRID fail-loud) |
| Alpha | `web/lib/risk-graph/surfaceModel.test.ts` |
| Charlie | `web/lib/risk-graph/surfaceScene/index.ts` |
| Charlie | `web/app/app/options-lab/surface/page.tsx` |
| Charlie | `web/components/options-lab/surface/SurfaceApp.tsx` |
| Charlie | `web/components/options-lab/surface/hostLaw.ts` |
| Charlie | `web/components/options-lab/surface/hostLaw.test.ts` |
| Charlie | `web/lib/optionsLabSuite.ts` (`surface` pill) |

No migration. No `3d/alpha.js`. No Backtest seed. No W3-2 camera HUD.

---

## Tests

```
npx tsx lib/risk-graph/surfaceModel.test.ts  → ok
npx tsx components/options-lab/surface/hostLaw.test.ts  → ok
```

| Id | Evidence |
|----|----------|
| T-IV-1…4 | bind `vix` / missing / `nearest`+`atm_exp` → IV NO; exact+locked ok; 0.005 keep |
| T-WIN-1/2 | `timeAxis[0]===tauHi`, last===tauLo; tauHi past remaining life throws |
| T-LM-1 | `MIN_TAU` = 1/365.25/24/60; comment cites OPF29 AT-L0-τ1/τ4 |
| T-LM-2 | `hostLaw.test.ts`: 16:01 ET → residual |
| T-LM-3 / T-TM-2 | wing `vix` / missing exact → IV NO |
| T-GRID | nx/nt Infinity throws unbounded |

---

## Curl

Host: `http://localhost:3001` (dev; :3000 is an older `next start` without this route).

| Request | Result |
|---------|--------|
| `GET /app/options-lab/surface` | 200 member shell (`data-testid=surface-host`) — same class as Analyzer |
| `GET /api/me/profile` no cookie | **401** `{"detail":"Sign in required"}` — not a public data leak |

---

## Desktop + phone walk (Playwright Chromium)

`?probe=iv-no` and `?probe=tent` on the **same host**.

| Viewport | Host | IV NO | Tent |
|----------|------|-------|------|
| 1280×800 | height 630 · pill Surface | `data-state=IV NO` inside `surface-host` | `data-scene=ok` · 1 canvas |
| 390×844 | height 608 · pill Surface | same | `data-scene=ok` · 1 canvas |

Empty book (no probe): **WAITING** / “Show a listed structure in Analyzer.”

---

**Defects:** none that block W3-1. W3-2 not started.
