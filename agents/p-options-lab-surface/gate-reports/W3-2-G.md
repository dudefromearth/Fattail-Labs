# W3-2-G — Camera: persp, orbit, Slow zoom, Fit, ISO

**Verdict:** **PASS**  
**Date:** 2026-08-16  
**Agent:** Charlie  
**Depends:** W3-1-G  

---

## Files (seed only)

- `web/lib/risk-graph/surfaceScene/camera.ts` — pure pose math  
- `web/lib/risk-graph/surfaceScene/camera.test.ts` — **known-value fixture** + T-CAM-1  
- `web/lib/risk-graph/surfaceScene/index.ts` — perspective, orbit, Slow zoom, Fit, ISO  
- `web/components/options-lab/surface/CameraHud.tsx` — Echo words, ≥44pt, stopPropagation  
- `web/components/options-lab/surface/SurfaceApp.tsx` — HUD wire  

No market route. No persist. No playhead. No ortho HUD.

---

## Known-value fixture (`camera.test.ts`)

ISO eye **(1.65, 1.25, 1.65)** · Slow gain **0.5** · FOV **42**.

| Input | Expected |
|-------|----------|
| `orbitPose(ISO, 100, 0)` | eye **(0.6569590884221798, 1.25, 2.2390633658160493)** |
| `zoomPose(ISO, +100)` | radius **2.7530550303254016** (= r₀ × 1.04) |
| `fitPose` after orbit | ISO restored |
| `applyCameraInspect` | **compute calls = 0** (T-CAM-1) |

`npx tsx lib/risk-graph/surfaceScene/camera.test.ts` → **ok**

---

## Curl

`GET /api/me/pricing/package-quote` still **405** (POST only). No `server/routes/*surface*`.

---

## Desktop + phone walk (`localhost:3001` · `?probe=tent`)

| | Desktop 1280×800 | Phone 390×844 |
|--|------------------|---------------|
| HUD | Zoom Slow · ISO · Fit · 44pt | same |
| ISO | (1.65, 1.25, 1.65) gain 0.5 | same |
| Orbit +100px | matches fixture x=0.65696 z=2.23906 | same |
| Fit | restores ISO | same |
| Wheel +100 | r=2.753055 (Slow) | same |

HUD `stopPropagation` — cluster sits on the host, mesh still fills ~630px.

---

**Defects:** none. W3-3 not started.
