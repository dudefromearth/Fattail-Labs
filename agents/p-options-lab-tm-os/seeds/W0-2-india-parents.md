# Seed W0-2 — India parents

**Project:** Time Machine One Source  
**Agent:** India  
**Depends:** W0-0  
**Law:** spec v0.4 §3–4 · TMI-91…95 · A2_1 consume · SO-AR v0.8 §3 · TM v0.7.4 (supersession table)  
**Out:** product code · inventing a Labs film · treating Heatmap Redis or Width Fit Average as the TM hold

## Ask

Quote the tree (path + line), do not assume:

- `_book_hole` / `TODAY_LIVE` refusal
- `captureToday` call sites **into the TM hold** vs the TR14 live-generation ring Average uses
- `seedTodayFromSession` (archiveLoad) — W1 would make this live; W2 must dispose it
- `loadTmDay` today → `engageTodayFromCache`
- two-slot occupancy in `tmSlots`
- Heatmap Redis generation cache vs TM hold (still not the same)

Confirm parents. Confirm TMI-82…95 supersede the named v0.7.4 IDs and do not erase Coach §0. Confirm VIX is ruled (not an open question). Confirm this GO consumes A2 marks and does not build `/api/marks`. Confirm `day_changed` is in-flight only. Confirm §13a is untouched.

APPROVED or RETURNED for build readiness.

## Done when

Review in `agents/p-options-lab-tm-os/reviews/W0-2-india.md`. Coach Content Law. Bench delta.
