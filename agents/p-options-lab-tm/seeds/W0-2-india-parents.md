# Seed W0-2 — India parents / as-built

**Project:** Options Lab Time Machine  
**Agent:** India  
**Depends:** W0-1  
**Law:** TM **v0.7.4 BUILD AUTHORITY** · TMI-79 two slots · TMI-42 · Arch 28 · OT-EF v1.1 · SO-AR v0.8+A1 · plan **v1.2**  
**Gate it feeds:** W0-G  
**Out:** product code · Basic chrome · TPO · 1× · Spaces · Factory · MiniTwo · a `server/` film module · Redis as TM film · reopening §12.1–12.6 · a `heldDay: Date | null` occupancy design

## Ask

**APPROVED** or **RETURNED**. Label findings **BLOCKING** vs **ADVISORY**.

Must say, first:

1. One surface, one playhead owner, two **derivations**, two **browser slots**. Not two products. Not two cursors.
2. **TMI-79:** today always capturing; at most one archive day. Capture does not pause. Today is not discarded because a past day was opened.
3. **Two blobs, one playhead.** Opening Tuesday does not fork a second cursor. Hosts bind one `t_ms`.
4. **Return to live** drops the archive slot and parks on **today’s newest**, not a leftover Tuesday playhead.
5. **Switching Tuesday → Monday** discards Tuesday **before** Monday is accepted.
6. **Today’s slot is not “held for replay” in the TMI-79 sense.** It is the continuous capture. Scrubbing today windows that slot; it does not occupy the archive slot.
7. A design that is `heldDay: Date | null` (one occupancy variable) is **BLOCKING** — it reintroduces the v0.7.3 wording bug.
8. Heatmap Redis generation cache is **not** this cache. Two slots are browser. Do not “help” by writing archive days into Redis.
9. **As-built quotes** (path + line). Minimum seats: toolbar grid (Strikes/in already left of Autofit; PiP between Autofit and TM strip); `AnalyzerTimeMachineStrip`; `algoDayReplay.ts`; `algo_replay_path.py` `ohlc_1m` leftover; What-if `timeMachineEnabled` misnomer; TR14 `getStreamBook`; `tests/tm_archive_contract.py`. Spec §11 `ml-auto` row is stale if still present — quote the tree.
10. **§12 is a record.** Do not reopen 12.1–12.6. §12.10 is a label question, not a fourth collision.
11. Cadence (§12.4) and corpus bytes recorded from disk, or named holes.
12. Parents *confirm* filenames by reading `Specs/`.
13. Cache holds **no member identity**.
14. Leftover boards PARKED (`PARKED.md` on `p-options-lab-tmi` and `p-az-atm`).

A table without file quotes is **RETURNED**.

## Done when

Written verdict. No product code.
