# W5-1 — Charlie client consume (STUB)

**Agent:** Charlie  
**Depends on:** W4-G  
**Plan phase:** W5  
**Status:** Expand after W4-G. Do **not** fire now.

## Intent (locked)

Analyzer + Builder **consume** the OPF envelope. Clock / raw `/session-status` cease to be member SoR when the envelope is present. Chrome uses **W1 Echo labels**. Additive book stays independent resolve + sum.

## Forbidden

Merge-all-visible into one custom OPF (NX9). Inventing labels. Extra WebSocket.
