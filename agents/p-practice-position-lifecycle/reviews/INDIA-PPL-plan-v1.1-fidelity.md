# INDIA — Plan ⟷ spec fidelity (Advisor §1 / §2.5)

**Date:** 2026-09-14  
**Subject:** Plan v1.1 vs Spec v0.1 DRAFT  
**Disposition:** **CONFIRMED** for the Advisor’s gap. Not a Phase 2 re-review of the spec.

## Coach content intact?

Yes. Spec not edited in this pass except the §13 pointer to plan v1.1 (already landed with the plan fold).

## Fidelity

| Check | Result |
|-------|--------|
| Spine: matcher frozen; slot SoR; gates+409 on API | Plan and spec agree |
| PPL0-A* (Grok) | Plan names them; spec v0.1 already has GET `/opens`, write-path table, OD-25, two Coach phrases. `findPairedOpen` is the remaining PPL0-1 fold |
| “Accounts are highest / registry naming open” | **Not in spec v0.1.** Grep: no `highest`, no Position Registry, no accounts-vs-campaigns SoR. `practice_campaign_id` is Trade Log passive host |
| Fourth tree | Plan governance, not spec domain. Conscious tick on `PPL0-W0` |
| PPL3 caller audit | Not in spec (API law is the gates). Plan v1.1 / PPL3-4 is the right home |

## Blocks

None for stamping **on fidelity grounds**. Spec remains DRAFT until the token.

## Opinions

PPL0-1 should still add `findPairedOpen` to spec §5.1 (Grok PPL0-A2). That is already in the PPL0-1 seed.
