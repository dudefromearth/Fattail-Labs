# P3-G Delta

**Verdict:** **PASS** with residual · 2026-09-06

- `quantApi.ts` types `QuantControls` / `ControlsGrid`; `fetchQuantControls`.
- `MonteCarloLab.tsx`: segmented chips from the grid endpoint; `abs` chips $0.30/$0.50/$1.00; `legged` labelled contrast; defaults marked; `fill_model` / `fit_id` / K / abandoned legs on the face; no free field for friction controls.
- Browser walk against the restarted API is residual (no browser tool this pass). Member-facing copy matches W0-5/W0-6.
