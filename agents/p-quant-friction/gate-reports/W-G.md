# W-G Delta

**Verdict:** **PASS** · 2026-09-06 · **unfitted close**

Fail-closed §10: no fill better than limit in tests; unfitted complex is window hazard; off-grid refused not clamped; `legged` not default; null-bid law in engine; mark series unchanged; no fitted parameter typed by hand; no history rows; no depth claim; no mean; cell ceiling is control tuples; `regime_factor` does not scale unfitted `h`; tick declared in `friction.py`; store/builder/archive not touched.

**Fitted vs unfitted:** **unfitted.** P4 skipped (OD-ATRV-13 Not yet). `fill_model: unfitted_pessimistic`. No VIX fit. No depth.

ATRV **v0.11 as built** landed. Quant tests 40 passed. Classified nine non-quant fails are not this board.

Residual: live API restart + authenticated curl + in-browser walk of the chips.
