# R1 — blessed list execution (2026-09-20)

**Authority:** Coach APPROVAL R0 blessed list.  
**H1 does not start.** R0-3 / R0-4 / R0-5 remain gated on REQ-003 AP-1. R0-7 HOLD (SODP-MB).  
No “done.” Completion per item below.

ATs: `46 passed`  
`pytest tests/test_req007_hygiene.py tests/test_futures_history_hygiene.py tests/test_vp_display.py tests/test_ops_dash.py tests/test_sa_dev_api.py tests/test_vp_api_http.py`

chain_feed pid **538** RSS **71088** idle throughout every StudioOne-adjacent packet.

---

## R0-1 — stale comments (`web/lib/saChartStyle.ts`)

**Status:** complete (comments only).  
**Machine:** StudioTwo (repo). StudioOne untouched.

| | |
|--|--|
| Change | 90d / REQ-001 comment → N-bar lookback (REQ-006). “custom series” comment → L2 shares L1 scale. `minBarSpacing: 0.05` unchanged. |
| AT | `test_r0_1_no_stale_90d_or_custom_series_comment` |
| Grep | `90d` / `custom series` / `REQ-001` absent in `saChartStyle.ts` |
| Rollback | restore the two comment blocks in `web/lib/saChartStyle.ts` |

---

## R0-2 — print-path 90d gate DELETE

**Status:** complete. Precondition held with evidence, then gate deleted.

**Precondition — member OHLC never falls through inprocess:**

| Evidence | Result |
|----------|--------|
| StudioTwo `.env` | `LABS_HISTORY_API_BASE=http://192.168.1.111:4012` |
| MiniTwo plist | `LABS_HISTORY_API_BASE=http://100.74.220.38:4012` |
| Live hop `GET /api/app/vp/v1/ohlc/ES?tf=5m&contract=ESZ2026&bars=50` | **200** `price_source=massive_futures_aggs` `vendor_ticker=ESZ6` `bound_symbol=ESZ2026` — **no** `req001_min_days` |
| AT | `test_member_ohlc_hops_not_print_path` — `_history_hop` used, `ohlc_for_source` not called |

**Delete:** `_REQ001_MIN_DAYS`, `req001_min_days`, 90d SHORT HISTORY stamp on print-path `ohlc_for_source`. Print path still COMPLETE/GAPPED/WARMING. Member hop unchanged (history sidecar). Admin-dev `/api/dev/sa/v1/ohlc` no longer 90d-gates.

Hygiene: `test_calendar_window_deleted_on_provider_and_surface` now greps `server/sa_dev/service.py`.

**Rollback:** restore `_REQ001_MIN_DAYS = 90` and the two `span_days <` blocks in `server/sa_dev/service.py`.

---

## R0-6 — live pane named; retired chrome deleted

**Status:** complete. `:5055` **not bounced** (pid **54391** throughout).

**Live pane (named in code):** StudioOne Chain Snapshot `http://studioone.local:5055` / on-box `http://127.0.0.1:5055` · `GET /api/vp-ops`. Constant `market_data.ops_dash.LIVE_PANE`. `python -m market_data.ops_dash` still exits 1 pointing at that URL.

**Deleted (grep-proof in live package):**

- `server/market_data/ops_dash/page.py` (retired HTML)
- `agents/p-volume-profile-service/install/ai.fattail.labs.vp-ops-dash.plist` + `.run.sh` (reloadable copies)

**Kept:** `snapshot.py` (served by `:5055`), `install/retired/` archive + README.

AT: `test_live_pane_named_and_retired_html_gone`.

**Rollback:** restore `page.py` and the two install files from git; `LIVE_PANE` can stay.

---

## HOLD (confirmed, not executed)

- **R0-7** StudioTwo `chain_feed` pid **99058** — untouchable until named SODP-MB.
- **R0-3, R0-4, R0-5** — gated on Coach REQ-003 AP-1.

---

## Rider — MiniTwo structure overlay (named; not R1 refactor)

**Status:** complete. CP-1 FULL DRESS.

| | |
|--|--|
| Overlay | `server/routes/vp_display.py` + `sa_dev.py` → MiniTwo `~/Fattail-Labs/` |
| Was | HTTP 500 `"bins leaked into structure"` |
| Now | 422 `BINS_LEAKED`; sessionless hop uses StudioOne ceiling (already on `:4010`) |
| Bounce | `gui/$(id -u)/ai.fattail.labs.api` only (pid 7718 → **13137**). Next `:4001` pid **7260** untouched |
| PP-1 production API `127.0.0.1:4000` | sessionless off **200** `session_date=2026-09-18` no bins; sessionless on **200** 258 bins; dated 09-17 **200** |
| H0-2 | verifies on MiniTwo: no 500 |
| chain_feed | pid 538 RSS 71088 idle BEFORE and AFTER |
| Rollback | `cp ~/Fattail-Labs/server/routes/vp_display.py.bak-struct-20260920 ~/Fattail-Labs/server/routes/vp_display.py` (same for `sa_dev.py`) then `launchctl kickstart -k gui/$(id -u)/ai.fattail.labs.api` on MiniTwo |

---

## Rider — calendar refresh (promoted; independent of slate picks)

**Status:** complete on **StudioOne labs DB** (capture SoR). 360 Massive reference GETs, 28.3 s. No Redis interest keys.

Monday 2026-09-21 is listed for SPY/QQQ/IWM/GLD/XLF (daily) and MWF names (AAPL…). Last week Mon–Thu capture was SPX+XSP only because calendars were August.

VIX/VIX1D left as reference (no fake chain).

**Not this packet:** StudioTwo / MiniTwo `market_symbol_universe` (different MySQL hosts). Capture starvation is StudioOne.

**Rollback:** no snapshot of prior JSON; restore would be a prior dump if one exists. Capture follows the new dates at next session.

chain_feed pid 538 RSS 71088 idle BEFORE and AFTER.
