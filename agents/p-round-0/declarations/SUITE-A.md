# DECLARATION — Suite cluster A

**Coach (verbatim):** FIX A — retarget to fattail-foundations (or make fixtures
create their own course). Do not restore the old seed. Then proceed B–E
with verdict + apply + re-run. Stop if any E item looks like KILL FEATURE.

**Files:** `server/tests/test_catalog.py`, `test_lesson_gating.py`,
`test_member_progress.py`, `test_video_signed.py`. No seed restore. No MiniTwo.
