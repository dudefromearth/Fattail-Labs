# Gate reports — Round 0 bill

Evidence for `RB-*` items after a Coach `GO: RB-xx` (DL-329).

One file per ID: `<ID>.md`. Ternary verdicts where a Delta gate applies;
otherwise the executor files the exit-criterion evidence here.

MiniTwo is untouched unless that GO names MiniTwo.
