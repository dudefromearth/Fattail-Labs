# Seed A2-W0-3 — Mike auth

**Project:** p-studioone-archive-read (A2 strip)  
**Agent:** Mike  
**Phase:** W0  
**Depends:** W0-1  
**Law:** v0.8 §6 + A2_1 · plan v1.0  
**Gate it feeds:** W0-G · W4

## Ask

1. New route is the **same Bearer class** as `/api/coverage`, `/api/index`, `/api/fetch`.  
2. HTML `/` and `/api/status` stay open.  
3. 501 `ARCHIVE NOT CONFIGURED` ≠ 401 `ARCHIVE AUTH`.  
4. Labs member path session-only. No new cookie. No browser → StudioOne.

## Out of scope

Product code this wave. Tool-gate on replay.

## Done when

Review note in `gate-reports/`. No implementation until W4.
