# Seed A2-W0-4 — Hotel

**Project:** p-studioone-archive-read (A2 strip)  
**Agent:** Hotel  
**Phase:** W0  
**Depends:** W0-1  
**Law:** A2_1 · OT-EF · plan v1.0 FP-A2-3, FP-A2-5  
**Gate it feeds:** W0-G

## Ask

1. **`MARK GAP` is named.** Never last-observation-carried-forward. A stale VIX that moves plausibly is the failure this spec exists to prevent.  
2. Null `generation.vix` is **not** a hole. The tape supplies the value.  
3. VIX is a **different instrument**, not a second SPX spot. Serving marks does not re-open the two-truths problem v0.8 §9 item 2 claimed.  
4. 0DTE empty `mkdir` stays **NOT TODAY**, not a fake book.

## Out of scope

Product code. Tap writing `generation.vix`.

## Done when

Hotel note. BLOCKING if the plan or a seed proposes LOCF or waiting on `generation.vix`.
