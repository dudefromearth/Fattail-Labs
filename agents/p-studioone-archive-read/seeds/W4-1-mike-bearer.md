# Seed W4-1 — Mike Bearer

**Agent:** Mike  
**Depends:** W3-G  
**Law:** SO-AR v0.8 + Amendment A1 · plan v2.1 · spec §6.2 · AT-SOAR-22, 27, 28

Archive routes on StudioOne require Bearer once live. Dash `/` and `/api/status` stay LAN-open. Labs sends Bearer when configured. 401 is not empty coverage. Token ≥32 when present.

Design + tests. Coordinate with Alpha; do not bounce StudioOne.
