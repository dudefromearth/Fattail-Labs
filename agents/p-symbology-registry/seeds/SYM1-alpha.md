# SYM1 — Registry service + API

**Depends:** SYM0-G PASS  
**Agents:** Alpha, Kilo  
**Machine:** StudioOne. **CP-1 verbatim in the GO.** Outside RTH if the step could touch capture.  
**Rollback:** the single command that removes the service/unit.

Implement spec §4 only. PP-1 artifacts for initial rows. D6/D7/D8 unanswered. VPS Q1 untouched. Seed details after SYM0-G.
