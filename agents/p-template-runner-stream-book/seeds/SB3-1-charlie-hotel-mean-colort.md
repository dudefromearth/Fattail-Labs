# SB3-1 — Mean colorT

**Project:** Template Runner Stream Book  
**Agent:** Charlie · Hotel  
**Depends:** SB3-0  
**Feeds:** SB3-G

Average = mean of existing per-width Width Fit aggregates (median / `colorT` by column) over last W book slots. Ranking sheet maps those to 0–100 bars. Skip nulls; low \(n\) is not a fake #1. Not RGB. Not a second efficiency formula. Not `widthFit.ts` internals. Hotel: not a forecast. Batman kicker only if `bw-fly` is bound.
