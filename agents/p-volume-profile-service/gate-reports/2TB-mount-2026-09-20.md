# 2TB print-store mount — PP-1 2026-09-20

**Verdict:** volume is **mounted and in use**.

| Check | Evidence |
|-------|----------|
| Mount | `/dev/disk5s1` on `/Volumes/FatTail2TB` (writable) |
| Collector root | `LABS_MARKET_DATA_ROOT=/Volumes/FatTail2TB/fattail-market-data` in vp-futures run.sh |
| Ingest | ES trades `day=*` present (11 sessions); coverage.json floor/ceiling queried at serve time |
| On-box leftover | `/Users/ernie/fattail-market-data/vp/futures_history` (OHLC cache only) |

**VPS Q1 still needs (beyond the mount):** ~90-day per-contract print depth (today ~11 sessions on this volume); ES/MES **model** ACTIVE still blocked; engine backfill of older sessions if the disk holds them but coverage.json does not list them.
