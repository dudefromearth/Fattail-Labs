# A12 follow-up — hop pin + row= loud · 2026-09-18

## 1. `row=` was echoed, not applied

**Was:** `/range?row=1` returned `vp_row=1.0` and **501** native 0.25 bins. Silent conformance gap.

**Now:** SA-L8 render-only rebin on `/range` and `/profile/{kind}`.

| `row=` | Result |
|--------|--------|
| omitted or equal to substrate | native bins, `approximation: none` |
| integer multiple of substrate (e.g. 1.0 on 0.25) | coarser bins, volume conserved, `approximation: display_rebin`, `substrate_vp_row` |
| finer than substrate | **422** `row_below_substrate` |
| not a multiple | **422** `row_not_multiple_of_substrate` |

Detection store unchanged. Tests: `test_display_rebin_*`, `test_range_row_rebins_not_echo`, `test_range_row_below_substrate_422`. Cluster 24 passed.

StudioOne production API is **not** bounced this packet (CP-1 / RTH). Code is on `origin/main`; install per footprint (tonight autorun or RTH-safe). StudioTwo DEV sidecar recycled (`:4010` PID 967 → 83458). `:3000`/`:4000` untouched.

## 2. Hop latency

`studioone.local` mDNS answers **two A** (`192.168.1.111`, `192.168.68.57`) **plus AAAA** (ULA + link-local). Python `getaddrinfo` walks the dead/slow ones first (~1.2 s). ICMP to 192.168.1.111 is 0.7 ms; the 300× gap was not payload.

| Target | New-conn p50 | Keep-alive rest p50 | First request |
|--------|-------------:|--------------------:|--------------:|
| `studioone.local` | **1227 ms** | 2.7 ms (after the stall) | **1290 ms** |
| `192.168.1.111` (LAN pin) | **4.1 ms** | **1.8 ms** | **5.0 ms** |
| `100.74.220.38` (Tailscale) | **8.5 ms** | **3.8 ms** | **30 ms** |
| `studioone` (MagicDNS, not `.local`) | **12.5 ms** | **5.3 ms** | **17 ms** |

**Canonical (DEV-API.md):** LAN `http://192.168.1.111:4010` · MiniTwo/Tailscale `http://100.74.220.38:4010`. Never `studioone.local`. `vp_client` now HTTP/1.1 keep-alive.

StudioTwo `.env` `LABS_SA_DEV_VP_API_BASE` pinned to the LAN IP (file-read; no `:4000` recycle).
