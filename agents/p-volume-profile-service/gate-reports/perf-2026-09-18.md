# VP serving performance — 2026-09-18 10:50 ET

## 1. Hop

| Path | BEFORE | AFTER |
|------|--------|-------|
| `studioone.local` `/range` first | 1283 ms | 1168 ms (still don't use `.local`) |
| LAN `192.168.1.111` `/range` keep-alive | first 7 / rest p50 3.8 ms | first **25.5** / rest p50 **7.2 ms** · ETag on |
| On-box S1 localhost (earlier) | ~3.8 ms | — |
| Target | rest within ~10× on-box | **7.2 / 3.8 ≈ 2×** PASS |

`api_base()` rewrites `.local` → `192.168.1.111`. Keep-alive pool + uvicorn 75 s.

## 2. A14 ETag

| Surface | Status |
|---------|--------|
| DEV sidecar `:4010` | landed earlier; 304 proven |
| StudioOne prod `:4010` | **now** (PID 62256) ETag `"33e0dde87c71f62b"` on `/range` |
| Labs `/api/vp/v1/*` | copies ETag / Cache-Control |
| Live range/developing | `must-revalidate` (RTH) |

## 3. Redis hot (db 2)

`LABS_VP_HOT=1` · `/2` · 64 MiB app cap · no Redis CONFIG SET. chain_feed PID **538** untouched. OHLC warm **3 ms** (was 891–978 cold gzip).

## 4. Cold member-route waterfall (`:4000` → pin → S1)

| Request | BEFORE ms | AFTER cold | AFTER warm |
|---------|----------:|-----------:|-----------:|
| `/api/dev/sa/v1/health` | 6339 | **27** | 10 |
| `structure` developing | 7 | 33 | 11 |
| `ohlc/ES?tf=5m` | 891 | 978 | **3** |
| `range` full-span | 22 | 10 | 14 |
| **SUM** | **7258** | **1048** | **38** |
