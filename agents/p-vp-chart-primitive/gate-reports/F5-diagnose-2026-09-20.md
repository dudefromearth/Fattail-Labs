# F5 diagnose — candle / time-axis path (read-only)

**Date:** 2026-09-20  
**Machine:** StudioTwo client + Labs API `:4000` + StudioOne history `:4012`  
**Authority:** Coach F5 priority amendment. VP / CL-1 **wait**.  
**AP-1:** Coach’s screen. This report does not close F5.

Coach (RL-1): the time interval is broken in every mode, including Full History. Fix candles + date axis first.

## Packet steps

### 1. Seeded A22 vs fresh house

House (`defaultPrefs`): `priceTf=5m`, `profileMode=visible-range`, `mode=morning`.

Live `identities.sa_surface_json`:

| identity | priceTf | lawful | profileMode | mode |
|----------|---------|--------|-------------|------|
| 273 (Coach) | `5m` | yes | `visible-range` | morning |
| 13844 (zztest-a22) | `15m` | yes | absent | entry |

**Guess (old persisted interval choking new init) is false for Coach.** Stored tf is the house default. `mergeLoaded` does not coerce unknown tf — a `90d`/`4h` seed *would* 422 — but no such seed exists on this account.

`GET /api/me/sa-surface` as identity_id=0 (dev-login) is **400** `No identity for this session`. Coach SSO is identity 273, not that path.

### 2. Network trace of OHLC (after history print-tail repair)

Member-shaped `GET /api/app/vp/v1/ohlc/ES` with administrator session, Labs API `:4000` hop to StudioOne `:4012`:

| query | HTTP | bars |
|-------|------|------|
| `tf=1m` | 200 | 5000 |
| `tf=5m` | 200 | 5000 |
| `tf=15m` | 200 | 5000 |
| `tf=1h` | 200 | 2017 |
| `tf=1d` | 200 | 144 |
| `tf=5m&contract=ESZ2026` | 200 | 5000 |
| `tf=90d` / `tf=4h` | **422** `tf must be 1m\|5m\|15m\|1h\|1d` | — |

`t` is unix **ms** (13 digits). Last 5m close **7725.25**.

**Before the print-tail repair:** every `/history/v1/ohlc/ES?tf=*` was **500**. stderr:

```
File ".../futures_history.py", line 139, in _print_tail
    bars, *_rest = assemble_bars(chunks)
TypeError: assemble_bars() missing 2 required keyword-only arguments: 'from_d' and 'to_d'
```

That 500 is why no interval resolved, no bars arrived, and no time scale existed — Visible Range and Full History share one OHLC path.

### 3. Build-hash

| Surface | Evidence |
|---------|----------|
| StudioTwo API `/api/health` | 200 `git_sha=608e131f…` `env=dev` |
| StudioTwo Next `:3000` | `npm run dev` pid **25784**; page **200**; webpack HMR (`/static/chunks/webpack.js`) |
| `.next/BUILD_ID` | `ppGwgamW4c9FWyeS56Azr` (stale production artifact, chunks dated Sep 13) — **not** what `:3000` is serving |
| S1 history | pid **63127** after kickstart; `chain_feed` **538** unchanged; file overlay `_print_tail` kwargs; checkout HEAD `6e65104f` dirty on that file only |

### 4. Interval init

`SaPriceChart` OHLC effect keys `[source, prefs.priceTf, apiBase, contract]`. Utility bar only offers `SA_INTERVALS` (`1m|5m|15m|1h|1d`). Changing interval refetches the same endpoint that was 500ing. After repair, those tfs 200.

No client change is required for F5 first mark **if** Coach’s tab loads the live hop. A hard refresh is the AP-1.

## Verdict

F5 root defect = **OHLC host 500**, not A22 seed, not Visible Range, not Full History chrome.

Repair already serving on S1 (one kwargs line + test). Not on `origin/main`. VP / CL-1 **not started**.

## Start-over (Coach: revert known-good, then one step)

Kwargs patch on `_print_tail` **reverted**. Print tail is **off** the host path (`return []`). Known-good host = Massive aggs only. Tail is a later named packet.

S1 history kickstart pid **63731**. `chain_feed` **538**. `GET /history/v1/ohlc/ES?tf=5m` **200** n=5000 `tail_source=none`.

## UI vs server (same URLs the chart builds)

Browser path = Next rewrite `/api/:path*` → Labs `:4000`. Session cookie. Chart URL: `/api/app/vp/v1/ohlc/${source}?tf=${priceTf}` plus `contract=ESZ2026` once the picker fills.

| Check | Server | UI ingest (`toCandles` = wick + `t/1000`) |
|-------|--------|-------------------------------------------|
| `GET .../ohlc/ES?tf=5m` | 200, 5000 bars, `t` **ms**, keys `t,o,h,l,c,v`, `tail_source=none` | 5000 candles, 0 wick drops, 0 time collisions, LWC seconds → **2026-09-18T20:55:00Z** |
| same + `contract=ESZ2026` | identical 5000 | identical |
| `tf=15m/1h/1d` | 200 | all bars honest |
| Next `:3000` rewrite 5m | 200, 375450 bytes, 5000 | same body as API |
| `GET .../contracts/ES` | 8 rows, ids `ESZ2026`…, **`last_trade_date` absent** | picker uses `rows[0]` → ESZ2026 (live-date pick never fires) |

**Isolation:** the empty date axis was the **server 500**. This payload is what the UI already knows how to paint. No field/unit mismatch on the candle path.

Residual (not F5 root): contracts omit `last_trade_date` — picker still lands on ESZ2026. Print tail remains off by F5 start-over.

VP / CL-1 still wait. AP-1 = Coach refresh, candles + date axis at 5m.
