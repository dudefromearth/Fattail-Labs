# REQ-007 phase A — Visible Range default + F4 replacement

**Date:** 2026-09-20  
**Authority:** Coach GO. Phase B (TV visual / POC/VA) **HOLD**.  
**AP-1:** Coach’s screen. This report does not close REQ-007.

## F4 diagnostic (before merge)

`agents/p-vp-chart-primitive/gate-reports/F4-race-2026-09-20.md`

Race was the first-load `/range` fallback when `getVisibleRange()` was empty. Primitive attach already precedes data. New mode kicks `/window` from OHLC `apply()` using `defaultTimeWindow` timestamps.

## What shipped (StudioTwo surface)

- Visible Range **default** (`saLayerStore` `profileMode: "visible-range"`, A22 persist).
- Chrome chip `data-testid=sa-profile-mode` names the mode; click toggles Full History.
- Bins: StudioOne `GET /v1/profile/{target}/window` via member hop; pan debounced 160 ms; developing edge still `/stream`.
- Client render-only. Old first-load `/range` fallback **deleted** from `SaPriceChart.tsx` (`rangeUrl` absent). `profileFetchPlan`: empty window → **wait**, never `/range`.

## ATs

| AT | Result |
|----|--------|
| `npx tsx lib/saVpBand.test.ts` | ok (`profileFetchPlan` wait/window/range) |
| `pytest tests/test_req007_hygiene.py tests/test_window_bins.py` | 6 passed |
| Grep: no client bin assembly; no `rangeUrl` in `SaPriceChart.tsx` | pass |
| Playwright `e2e/req-007-visible-range.spec.ts` (dev-login, **zero mouse** before assert) | **2 passed (23.4s)** — `data-vp-bins > 0`, mode Visible Range, `/window/` hits, **zero** `/range/` on first load; toggle names Full History |

## CP-1

No StudioOne overlay. Window/stream already live. chain_feed pid **538** idle after surface work.

## Phase B HOLD

`artifacts/references/REQ-007-vrvp-reference.png` + Coach POC lean. Not this packet.
