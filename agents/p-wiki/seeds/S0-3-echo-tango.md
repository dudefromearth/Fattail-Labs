# S0-3 — Empty copy, density, no gamification (Echo + Tango)

**Plan:** Member Wiki v0.1 Full Agent Bench Plan v2.0 · **GO S0**  
**Depends:** S0-2  
**Isolation:** no compile inbox on `/app/wiki`.

## In scope

Review `/app/wiki` entry + `/app/wiki/[slug]` rail shell.

- Echo: search-first hierarchy; two-column desktop; mobile rail as collapsed accordions; house tokens; empty sections omit headings.
- Tango: Start here empty copy is honest (no invented pins, no profit claim); no gamification; practice slot hides (no upsell).

## Out of scope

Hover (S1). Related engine (S4). Practice fill (S6). Apps-card title (D-1).

## Completion

Written verdict in this seed. Block only for HIG/token invention or member-trust copy.

## Verdict (2026-08-23)

**Echo: PASS.** Search is the dominant control and autofocuses. Start here is a quiet card row (title + one-line tags). Article is two-column on desktop; rail uses house tokens and sentence-case section labels. Mobile rail is collapsed `<details>` (Linked from / See also); empty Related / In your practice headings do not render. No compile inbox. ⌘K uses the existing overlay dialog.

Opinions (not blocks): Apps card still reads “IKI Lab / Coming soon” (**D-1**). Cmd+K overlay does not dim the full page (pre-existing switcher). Body `## Related` in some git pages is content, not an empty rail slot.

**Tango: PASS.** No gamification, no “members also read,” no profit claims. Practice slot stays hidden (no upsell). Start here leads with position sizing (survival / stop-the-bleeding), then routine — same sequencing as the pathway. Honest empty copy remains in the entry page if pins are later removed.
