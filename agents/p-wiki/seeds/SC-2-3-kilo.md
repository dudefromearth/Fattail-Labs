# SC-2-3 — Kilo

`tests/test_wiki_source_contract_push.py`: L12 no retry no page; hash on
land + watermark; profit no page; admin-only API; extra schema fields 400;
panel has artifact+intent, no `source_kind`/`content_hash` inputs; session
still opens; AppChrome not imported.
