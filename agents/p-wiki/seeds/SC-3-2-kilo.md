# SC-3-2 — Kilo

`tests/test_wiki_source_contract_poll.py`: GET-only (POST raises); L10
signal-created + same hash → `hash_unchanged`, no git page; published change
→ envelope + watermark; missing signal still hashes; drafts skipped (L11).
