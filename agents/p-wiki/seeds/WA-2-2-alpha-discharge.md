# WA-2-2 — Oscar discharge (Alpha)

**Plan:** GO WA-2 · depends on WA-2-0

## In scope

`ai.complete(agent="oscar")` with Hotel guidelines in the system prompt. Pointer GET → draft.md `status: draft` → server git commit (contract id in message) → board `product_line=wiki` `awaiting_approval`. Retired → annotating draft, never delete the page file.

## Out of scope

Model credentials in git. Member UI. Auto-publish (W5).

## Completion

Draft in checkout; member 404; board card exists; profit-claim grep clean.
