# WA-3-2 — Board suggestion list (Charlie)

**Plan:** GO WA-3 · admin board only.

## In scope

Below-threshold candidates rendered on the existing admin board card (`intent_md` on `/admin` board — already displayed). No member wiki rail. No AppChrome.

## Out of scope

New board UI chrome unless required to show markdown already there.

## Completion

A card for an ingest lists below-threshold slugs with scores; those slugs are **not** in the draft file as `[[wikilinks]]`.
