# WU-1-6 — Kilo WU-1 characterization

**Plan:** Wiki Spec v0.2.1 · **GO WU-1** · gate table

## Proof rows

- Discovery file+lines quoted; mount = wiki layout only
- Hub provider enriches entity for route `/app`
- Unregistered surface = route-context (entity null)
- First turn cites `{surface, route, entity}`
- Accrete does **not** git-draft (propose-and-dispose)
- Draft path still WA-2 on explicit draft
- Rider-style both layers; `ftl_ag_` rejected
- `git diff --stat` AppChrome + `web/app/layout.tsx` empty
- Registry env fail-loud
- House box tolerated 8 only
