# REQ-003 F1 — Coach-ruled clauses (amendment 2026-09-19)

**Status:** OPEN (REQ-003 stays OPEN). Not AP-1.  
**Machine:** StudioTwo.  
**Authority:** Coach. Transcribed same day (doctrine §14).  
**Replaces:** F1 clauses **2** and **3** only. Clauses **1, 4–6** and the screen-recording acceptance **stand as issued** — this file does not reconstruct them.

## Coach wording (RL-1)

> "full search" (tickers AND names both matched, substrings highlighted)
>
> "the current active and the forward contract are on top."

## Clause 2 — Full search (this amendment)

Every keystroke matches ticker AND plain-English name ("e-mini", "s&p", "500" all reach the ES family). Matched substrings highlighted in the rows.

## Clause 3 — Ordering (this amendment)

Ticker-prefix matches rank above name-only matches. Within a futures family, top rows are always: current front contract first, forward contract second (ES1! / ES2! alias forms shown against them), then remaining strip contracts chronologically. Front/forward read from the live strip as-of `strip_generation_id` — never hardcoded.

## Acceptance recording (additive)

Stands as issued, and now also shows: typing **"es"** → ES family on top, front contract as its first row, forward second, highlights visible.

## Closure

AP-1 remains Coach's browser, his clicks, StudioTwo: tile → dialog → selection → chart. Do not write "done."
