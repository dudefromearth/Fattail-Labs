# REQ-004 — Refactor sweep (after TOPO-1)

**Status:** OPEN. **HOLD** until TOPO-1 (SODP) AP-1. No R1 packet until Coach approves the R0 list.  
**Captured:** 2026-09-19.

**Coach wording (RL-1):**

> QUEUE: Refactor sweep + hardening pass — after the TOPO-1 migration completes
>
> Sequencing law: these run in order, each gated on the last: TOPO-1 migration complete (AP-1) → REFACTOR → HARDEN. No refactor packet before migration closes; no hardening packet before the refactor sweep closes. Board rows now, execution then.
>
> Phase R0 (inventory, read-only): sweep the Labs tree and both StudioOne planes for: dead code and orphaned routes (struck fill branch remnants, fixture leftovers, retired overlay/redrawVp traces, stale "L2/custom series"-class comments), duplicate symbol or config lists surviving outside the registry, hardcoded symbols/months/machines/ports, TS-1 candidates (components with one strike), and seam code left by SYM-SWAP and the migration. Deliverable: triaged packet list with effort estimates — I approve the list before any packet runs.
>
> Phase R1 (execute approved packets): acceptance per packet is BEHAVIOR-PRESERVING, proven — contract tests green before/after, and AP-1 in reverse: member-visible behavior unchanged on my screen. Grep-proofs for every deletion. Line counts deleted vs added reported (deletions should dominate, but per DL-766 that is a health indicator, not a gate).
>
> Open REQs, F3, TOPO-1, and the eligibility report retain priority — these programs start only when that board is clear.

**Closes:** AP-1 (reverse: behavior unchanged) after approved R1 packets, or Coach withdraw.
