# REQ-007 v2 — Visible Range profile, server-owned bins

**Status:** OPEN. Phase 1 client-side histogram **superseded**. Visual/POC HOLD until reference PNG + Coach ruling.  
**Captured:** 2026-09-20.

**Coach wording (RL-1):**

> implement the viewable range in TV as it is the most natural and will be easiest to diagnose issues
>
> visible range [named as the default mode]
>
> The server must always update the bins in real time.
>
> From TV

## Law (v2)

1. Bins are **server-computed, always**. Client never assembles a histogram from bars.
2. Developing edge streams on `/stream`. Closed market → holding-last.
3. Visible Range = **window query** (from_t / to_t). Windowed-delivery kind (G-F / §9.2) activated. Pan/zoom re-queries (debounced). Completed-session bins cached.
4. **Evidence-graded, coverage queried.** For any window the print store covers **for that contract** (coverage at serve time, per contract, **never a configured date**), bins are prints-true. Before that coverage, server derives from Massive aggs — `bin_source: prints | aggs-derived`. No date constant. No client derivation.
5. TV fidelity look/behavior; POC/VA HOLD until reference PNG on main and Coach rules the lean.

Mode: **Visible Range only.** There is no Full History member-chart mode, toggle, or chrome. Pan, zoom, logical-range (timeline), price-data changes, and `sa-vp-update` on the chart host all refetch `/window`.

**Closes:** AP-1 on Coach's clicks. Not closed.
