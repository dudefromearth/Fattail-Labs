# FatTail Labs — Strategy Lab Config Resolution Standard v0.1 (DESIGN)

**Status:** **FOLDED** into Guiding Doctrine Spec v1.0 **§16** and Arch **32** §6b
(**DL-384**, 2026-08-16). This file is the **source draft** (Coach text kept).
**Authority is the Spec.** Do not edit this file as a second SoR.
**Sits beside (DL-386):** classified VP is a **slow layer** (`profile_version`,
`structure_maturity`) — not rebuilt at the 3–5s gold cadence of the per-leg vol
surface in §1. See SL-GD **§17.7**.
**Parents:** SL Guiding Doctrine v1.0 (DL-382) · Method v0.2 (MC, frictions, atomic
fills, scope-bounded retry) · Arch 32 (lenses, tile → pack, no second engine) ·
Analyzer per-leg vol surface (the modeler) · Position paper (position, don't predict).

---

## 0. The principle in one sentence

**A strategy is not a trade; it is a rule that resolves to a trade against whatever
surface it meets.** The config describes the *shape of the seat* relative to the
market. The market supplies the concrete strikes, debit, and marks — and it supplies
different ones every day and every minute, because the surface moves.

Corollary: **no absolute value in a config that the surface will move.** A config with
a hard strike in it cannot be backtested, only replayed; it cannot be run live
tomorrow, only remembered.

## 1. The surface is a moving object

The 3D per-leg vol surface (strike × time × vol → price, greeks) is rebuilt for the
day and updated every tick (3–5s gold, per bar silver). It changes shape with:

| Driver | What moves |
|---|---|
| **Underlying track** | Spot walks; the tent's center relative to strikes shifts; EM-relative zones slide with it |
| **Per-leg vol** | Skew and term change leg by leg; the same fly reprices, its curvature and debit change, its R2R at potential changes |
| **Time decay** | The curve steepens through the day; a flat morning curve becomes the steep last-day curve (position paper §4 — two curves) |
| **Regime** | VIX / VIX1D shift the decay rate and the width that makes sense (premarket vector) |
| **Liquidity** | Spreads and sizes change what fills and at what friction |

The same config meets a different surface at 9:35, 11:00, 15:45 — and on Monday vs a
CPI Thursday. That is not noise to be smoothed; it is the thing being tested.

## 2. Config vocabulary — surface-relative by law

Every strategy field is expressed as a **rule the surface resolves**, never as a value
the surface will move. The designer's tabs map to these:

| Tab | Field class | Written as (surface-relative) | Never written as |
|---|---|---|---|
| Identity | species, family, direction | fly / BWB / Batman; call / put / both | — |
| Structure | DTE, width | DTE class (0 / 1 / 3–5+); width in **strikes** | width in points; a specific expiry date |
| Risk & Capital | max at risk, band | fraction of capital; **2–6% designed max DD** | a dollar strike price |
| Convexity & Debit | placement, debit, R2R | **zone** relative to expected move (e.g. "just beyond EM," delta band, σ band); debit as **fraction of width** or a range; **R2R at potential** floor; curvature preference | a strike; a debit in dollars alone |
| Timing & Entry | when | premarket regime condition → session window (morning / afternoon / close, **T−N** minutes) → entry trigger | a clock time with no condition |
| Exit Rules | how it comes off | fraction of **max profit** (potential); tent-relative levels; decay-trail; **opportunistic** vs **intense last-day** rule; stop as fraction of debit; hold-to-expiry rule | a dollar target |
| Review | evidence | MC distribution shape acceptance | a single path |

**Placement is a selector, not a strike.** The Convexity & Debit rule defines the
*eligible region* on any chain (zone × width × debit range × R2R floor × curvature
preference). The heatmap shows that region on today's chain for the member to preview.
The bot runs the identical selector automatically. **One selector, two callers.**

## 3. Resolution — how a config becomes a position

At every candidate moment the engine performs the same act, in order:

1. **Regime check** (premarket vector) — does today's vol regime admit this seat at all? If not, no attempts today.
2. **Window check** — is the session window open (morning / afternoon / T−N)?
3. **Surface read** — take the current surface (this tick's chain, per-leg vol, spot).
4. **Selector** — apply the placement rule to the *listed* chain: eligible zone → eligible widths → candidate structures; price each on the surface; rank by the config's convexity score (R2R at potential / curvature per debit — one function, the same the heatmap displays); pick the top eligible tile. **Only listed strikes; no invented geometry.**
5. **Scope test** — is the chosen structure inside all config bounds (debit range, R2R floor, band, width)? If nothing eligible → **no trade this moment**, recorded as an event.
6. **Attempt** — submit the atomic position at the config's price rule; fill friction rolls; **retry until filled**, each retry re-running steps 3–5 on the *next* surface; **abandon on out-of-scope** (Method v0.2 §5).
7. **Hold and manage** — the position marks on the moving surface every tick; exit rules evaluate against **potential** (fraction of max profit, tent-relative, decay-trail, last-day intensity); GEX as manage-time lens; exits obey the same retry/scope loop.
8. **Record** — one atomic trade event: resolved geometry, debit, R2R at potential at entry, fill path, marks, exit path, outcome bucket.

The config never changes during resolution. Only the surface does. **Same config, different day, different fly, same seat.**

## 4. What the Monte Carlo varies, and what it does not

| Fixed across runs | Varies across runs |
|---|---|
| The tape (surface track for the day) | Fill outcomes and prices (market friction) |
| The config | Retry timing → resolved geometry drift → entry debit |
| The selector function | Operator-friction events (late/missed/oversized), when dialed above 0 |
| Listed strikes | Which eligible tile wins when friction moves the ranking between retries |

Result: a **distribution of resolved trades** for the same seat on the same day — the
honest range of what "this strategy on this day" means once fills are real and humans
are human. Shape out; never a path.

## 5. Two-curve law inside resolution

Because the surface's *shape* changes with time, the same config must know which curve
it is on (position paper §4):

- **Flat curve** (days out, opened inside the tent): the mark is a gift; the
  opportunistic exit rule governs — fraction of max profit reached → take.
- **Steep curve** (last day, decay spent): intense management; the last-day rule
  governs — tent-relative and decay-trail exits, T-minus discipline.

The exit config names both; the engine selects by where the surface's curve is, not by
a clock. This is why exits are written as *potential-relative*: 300% on a flat curve
and 800% on a steep one are both "fractions of what this tent can pay right now."

## 6. Two-truth law for the designer

For the designer to be complete, the config it emits must satisfy both:

1. **A human can preview it** — the heatmap shows what the selector picks on today's
   chain; the risk graph shows the resolved structure on the current surface.
2. **A machine can resolve it with no human in the loop** — on any surface, any day,
   the same selector produces a position or a recorded "no eligible structure."

A config that only satisfies (1) is a sketch. A config that satisfies (2) is a strategy.
The designer is done when every tab's output is (2)-grade.

## 7. Provenance on every resolved trade

Each resolved position carries: config version + hash, surface tier (gold/silver),
tick timestamp of resolution, selector score at pick, listed geometry, R2R at
potential, per-leg vol at entry, friction model version, operator-friction level, MC
seed. Same record for backtest, forward walk, sim, and live — one trade shape, four
contexts (Method §6, "the bot is a member").

---

*Advisor draft of Coach's standard. Rules on Coach acceptance. Feeds the Designer's
completion criteria and the backtester's engine contract.*
