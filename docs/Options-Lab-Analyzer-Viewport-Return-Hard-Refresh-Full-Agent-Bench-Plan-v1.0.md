# Analyzer 2D Return vs Hard Refresh — Full Agent Bench Plan v1.0

**Date:** 2026-08-19  
**Plan revision:** **v1.0.1** (RH-B1 artifact citation · RH-A2 Coach-path-first on W0-M)  
**Owner (orchestration):** Juliet  
**Authority:** Coach (GO / ship)  
**Board:** [`agents/p-az-viewport-return/`](../agents/p-az-viewport-return/)  
**WHAT (law):** [`docs/Options-Lab-Analyzer-Viewport-Return-Hard-Refresh-Analysis-2026-08-19.md`](./Options-Lab-Analyzer-Viewport-Return-Hard-Refresh-Analysis-2026-08-19.md)  
**Governance:** [`agents/bench/doctrine.md`](../agents/bench/doctrine.md) · [`AGENTS.md`](../AGENTS.md) · first-principles (three strikes → original purpose)

**Juliet does not invent WHAT.** Coach’s splitter is the program:

> Works after a hard refresh. Stops working after moving away and coming back. That is it.

**Review fold (same session):** External plan review. **RH-B1** blocking (sequencing, same shape as VPP-B1) — stamped below. **RH-A1** reviewer could not see the WHAT from their seat; the WHAT is on disk at the path above (analysis §3 A–F · §4 R1–R10). **RH-A2** advisory (Coach path first on W0-M). Coach Content Law: original parent-table wording sits in this fold as the reviewer’s, then the table is corrected to cite the prior board’s **artifact**.

**RH-B1 (BLOCKING · folded):** This plan must not assert Packet A “closed” or “exclusive lock over” from its own parent table. **W0-1 Lima or W0-2 India cites the prior board’s W-G gate report, or its absence, explicitly.** As-disk on this machine:

| Prior artifact | Path | What it says |
|----------------|------|----------------|
| W-G | `agents/p-az-viewport-2d/gate-reports/` — **no `W-G.md`** | **Absence.** Packet A W-G has **not** filed. |
| W1-G | `agents/p-az-viewport-2d/gate-reports/W1-G.md` | “landed pending W2/W3/W-G” |
| ORCHESTRATOR | `agents/p-az-viewport-2d/ORCHESTRATOR.md` | W1-1 **in flight** · W-G **BLOCKED** |

That absence is not fatal. It means India **names the lock handoff** at W0-2 instead of inheriting “the lock is over.” Two boards must not hold overlapping exclusive scope (VP-B1 inverted).

**Parents (do not re-litigate):**

| Doc | Role | Artifact (not this cell) |
|-----|------|--------------------------|
| Prior viewport board `p-az-viewport-2d` | Packet A *intent*: Autofit lock + left-click pan + native wheel. **Cause of this splitter:** not Packet A’s original T8/menu (analysis R10). **Exclusive lock:** **not inherited as over** — India W0-2 names the handoff from the **W-G absence** above. Packet B stays on that board, still blocked on *its* W-G. | Cite `W1-G.md` + **no `W-G.md`**. Do not cite this table as proof. |
| Analyzer Spec v0.2.1 §1.14.3 | Left-drag pan · wheel zoom · right-click alerts | Spec file |
| Arch 28 | One WS/tab · no client Massive | Arch 28 |
| OT-EF · DL-309 | No invented strikes | Doctrine + DL title |

Specialists fire **only** from seeds. Delta: **PASS / FAIL / BLOCKED**. Never waived.  
No product code until **W0-M has a FAIL that matches the splitter** **and** Coach **W0-BA**.

---

## 0. Why this program exists

The previous viewport program treated intermittency as Autofit + alert-menu steal. Coach’s remaining report is a **binary process splitter**: hard refresh works; leave and return does not; the position is visible. Automated Chromium Heatmap/Surface-tab probes **pass** drag and wheel. Coach **fails**. The next work is to **make the splitter fail in a harness**, then fix **that** survivor — not to patch another guessed overlay.

---

## 1. Mission

```text
W0    Review this analysis (India · Echo · Tango · Hotel · Delta)
W0-M  Kilo — FAIL that matches hard-refresh vs return   ← blocks Charlie
W0-BA Coach BUILD AUTHORITY (after W0-G + W0-M FAIL, or Coach stamp + DL naming bypass)
W1    Charlie — fix only the survivor W0-M named
W2    Kilo — splitter test stays red on the old tree, green on the fix
W3    Lima DL + analysis status · Echo gesture (∥ W2)
W-G   Delta
```

---

## 2. Hard gates

| Gate | Rule | Unblocks |
|------|------|----------|
| **W0-1 Lima** | Hash WHAT + this plan. **RH-B1:** quote prior `W-G.md` **or write “W-G not filed; W1-G pending W2/W3/W-G.”** Not this parent table. | W0-2 |
| **W0-2 India** | Splitter is persistence (layout / module `let` / listeners / bfcache). No Market Bus second socket. No Packet B. **RH-B1:** lock handoff **named** (who may touch `PnLChart.tsx` / `OpfRiskAnalyzer.tsx` while `p-az-viewport-2d` W-G is unfiled) — not “lock is over” by assertion. | Echo · Tango · Hotel |
| **W0-3 Echo** | Gesture grammar unchanged: left-drag pan, wheel zoom, right-click alerts. Block only HI invariant. | W0-G |
| **W0-M Kilo** | **FAIL** on leave/return without hard refresh (drag **and** wheel), or **BLOCKED**. **RH-A2:** run **Coach’s path first** (W0-0 named A–F, Coach’s order) before permutations. A green seed-Chromium Heatmap probe is **not** PASS. | W0-BA (with W0-G) |
| **W0-G Delta** | Analysis + this plan + board on disk. **No product code.** W0-M filed. | Coach W0-BA |
| **W0-BA Coach** | BUILD AUTHORITY naming the W0-M survivor (R1–R7 or a new ID with evidence) | W1 Charlie |
| **W-G** | Splitter test FAIL on pre-fix / PASS on fix · docs · Echo W3 | Ship (Coach) |

---

## 3. Locked

- Do not declare a cause that does not predict: **hard refresh works · return breaks drag and scroll**.  
- Do not accept “Playwright passed” as Coach passing.  
- Do not unmount the 2D pane as a “fix” without W0-M (blank-pane regression is already in Coach history).  
- Packet A exclusive lock is **not** assumed over. **RH-B1:** India W0-2 names the handoff from `p-az-viewport-2d` **W-G absence**. This program may touch `PnLChart.tsx` and/or `OpfRiskAnalyzer.tsx` only as that handoff (and W0-BA) names. Two boards must not both hold exclusive scope on the same file.  
- One WS/tab. No client Massive. No invented strikes.

---

## 4. NX (not this program)

NX1 Packet B strike handles  
NX2 MiniTwo until W-G + Coach  
NX3 What-If W2 reopen  
NX4 Surface Autofit / T Ortho egg  
NX5 Heatmap tiles  
NX6 “While we’re here” inspector hydration  

---

## 5. Seeds (board)

See [`agents/p-az-viewport-return/seeds/`](../agents/p-az-viewport-return/seeds/).

| Seed | Agent | Fire |
|------|-------|------|
| W0-0 | Coach | Plan stamp |
| W0-1 | Lima | Hash + **RH-B1 W-G artifact (or absence)** |
| W0-2 | India | Persistence + **RH-B1 lock handoff named** (not TBD-by-silence) |
| W0-3 | Echo | Grammar |
| W0-4 | Tango | Member: dead pane after return |
| W0-5 | Hotel | Lying immovable graph |
| W0-M | Kilo | **FAIL the splitter** |
| W0-G | Delta | After W0-2…5 + W0-M |
| W0-BA | Coach | After W0-G |
| W1-1 | Charlie | After W0-BA |
| W2-1 | Kilo | After W1 |
| W3-1 | Lima | After W1 |
| W3-2 | Echo | After W1 |
| W-G | Delta | After W2+W3 |

W0-M may run **in parallel with** W0-2…5 once W0-0 is stamped (measurement is not product code). It **must** complete before W0-BA unless Coach’s DL names the bypass.

---

## 6. W0-M bar (Kilo)

A PASS on W0-M is **illegal**. The gate is:

- **Order (RH-A2):** Coach’s path **first** (W0-0 stamp: analysis §3 A–F, Coach’s order). Then remaining permutations. Tab-within-app, suite route, browser tab, Back, minimize are **different** lifecycles — do not guess which Coach used.  
- **FAIL** — leave/return, no hard refresh, position visible, **real** pointer drag does not change `viewX` **or** `dragTicks` stay 0; **and** real wheel does not change `viewX` **or** `wheelTicks` stay 0. Record path, hit stack, `inert`, `data-wheel-bound`.  
- **BLOCKED** — Coach’s path (or all A–F if unnamed) run green **and** Coach still fails. Then Coach records the path or sits with Kilo on the machine. Cheaper than a third guessed fix.

Harness rules:

- `page.mouse` / real wheel — **not** `host.dispatchEvent(new WheelEvent)` as the sole proof.  
- Seed must include **listed** Create 20-wide at spot **or** Coach’s book.  
- Dump JSON like `web/test-results/analyzer-hit-probe.json`.

---

## 7. W1 (only after BA)

Charlie changes **only** the survivor W0-M named. If W0-M names R1 (bind skipped), the fix is bind-on-host-commit (callback ref / `useLayoutEffect` bind) with a test that the host has `data-wheel-bound=1` after return. If W0-M names R4 (leftover canvas), dispose/hit-stack. **No second guess packed into W1.**

---

## 8. Status

| Packet | State |
|--------|--------|
| This analysis | **Landed · awaiting W0-0** |
| This plan | **v1.0.1 · RH-B1 folded · awaiting W0-0** |
| Board | **Landed** |
| W0-M | Not fired |
| Product code | **Forbidden** until W0-BA |
