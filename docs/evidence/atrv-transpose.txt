archive : /Volumes/FatTail2TB/fattail-market-data/ssr/live_capture   (read-only)
day     : day=2026-09-04   using 6,000 of 20,472 snapshots   book=XSP
scratch : /private/tmp/atrv-layout
codec   : zstd (python-zstandard, level 3)
pack    : 30 snapshots/file

building layouts …
verifying byte-roundtrip …

layout                    files    on disk      scan    vs A        rows
------------------------------------------------------------------------
A_baseline                6,000    137.4MB     1.30s   1.00x     409,480
B_compressed              6,000     34.4MB     1.41s   0.92x     409,480
C_packed                    200    137.5MB     1.14s   1.14x     409,480
D_packed_compressed         200     13.3MB     1.16s   1.12x     409,480

========================================================================
FASTEST: C_packed  (1.14x baseline)

  compression alone :  0.92x
  packing alone     :  1.14x

  -> NEITHER helps materially at this scale (best 1.14x). Do NOT write a mitigation
     into a spec on this evidence. Re-run cold (`sudo purge`) and with a
     larger --limit; if it still flattens, the cost is elsewhere.

  storage: A 137.4MB -> D 13.3MB
========================================================================

MARK-ONCE — pricing compute-as-you-collect
  138 structures over 400 snapshots
  0.04 ms per snapshot  =  0.002% of the 2 s capture budget
  -> FREE — do it in stream

TRANSPOSE — closed-day build of [C][T], the number that decides
            whether a nightly build is a problem at all

source                        read+parse+transpose      C       T  4-leg gather
-------------------------------------------------------------------------------
A  JSON storm (loose files)                  3.22s     51   6,000         1.54ms
C  packs                                     3.02s     51   6,000         1.63ms
D  packs + compressed                        3.10s     51   6,000         1.61ms

  packs vs JSON storm : 1.07x
  projected full day  : ~6 s from packs (6,000 snaps measured, scaled to 11,700)
  -> nightly transpose is CHEAP. Settled. Stop talking about it.


ANATOMY — where a snapshot's bytes go
  21.4KB per snapshot · 62 rows × 16 keys
    key names repeated on every row  40.2%   (8.6KB)
    values as decimal text           50.2%   (10.7KB)
    braces, commas, whitespace        9.6%   (2.1KB)
  same numbers as float32 columns : 2.9KB  = 7.4x smaller
  -> that ratio is the DERIVED store's headroom, not a reason to
     change the archive: verbatim (SSR-MEXP §3.1) is what keeps the
     corpus answerable to questions nobody has asked yet, and
     compression recovers the key repetition WITHOUT spending it.

wrote docs/evidence/atrv-transpose.json

cleaned /private/tmp/atrv-layout
