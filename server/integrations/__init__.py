"""Third-party integrations (brokerage connections, etc.).

Each integration is fail-closed: it stays inert until its own env config is present.
See integrations/tradier for the first (Tradier brokerage sync).
"""
