"use client";

// Historical component: draft handling for /courses/{slug} now lives in
// app/not-found.tsx so the editor owns the full page (no /admin redirect).
// Kept empty so old imports (if any) do not break the build.

export default function AdminDraftRedirect() {
  return null;
}
