"use client";

/**
 * Live Volume Profile is mounted by the member page.
 * Keep-alive stays a no-op so it cannot hide or double-mount the chart.
 */
export default function VpSurfaceKeepAlive() {
  return null;
}
