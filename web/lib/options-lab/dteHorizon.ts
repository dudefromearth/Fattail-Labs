/**
 * Catalogue key for the Analyzer / OPF active option horizon (calendar DTE).
 * This is the single declaration (PC-EXP-4). TS and Python import from here.
 * Curriculum-sized, currently 10 (PC-EXP-3).
 */
export const OPF_ACTIVE_DTE_HORIZON = 10;
